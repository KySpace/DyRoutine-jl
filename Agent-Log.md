- Request: Build a PCA helper in `src/corr.jl` for serialized image-like samples: accept an integer `n_mode` and an arbitrary-dimensional array whose elements are arbitrary-dimensional real arrays; return `n_mode` modes, each with a reshaped mode profile and a per-sample weight array matching the outer sample shape. Before implementation, compare using a custom `ModeWeight` type versus a `Dict`.
  Brief: Explained that `ModeWeight` gives clearer fields, type-stable downstream use, and a stronger API contract, while `Dict` is more flexible but typo-prone and less compiler-friendly. Added `ModeWeight`, `build_pca_matrix`, and `fit_pca_modes`; the wrapper flattens each inner sample as one PCA observation column, calls `MultivariateStats.fit(PCA, ...)`, uses PCA projections as profiles, uses `predict` for per-sample weights, and reshapes outputs back to their original profile/sample axes. Parsed `src/corr.jl` successfully; full runtime verification was blocked because this Julia install cannot load standard libraries such as `Pkg`/`SparseArrays`.
- Request: Refactor `script/anlz_miscibility.jl` so each `runinfo` reads all corresponding folders as one raw data block, formats by variables recorded together in a named tuple, removes `rep_each`, keeps number-stat plotting but comments out the full duet table, and makes `_fmt` data align with variable axes rather than appending image dimensions.
  Brief: Updated `runinfos` to use `vars=(IB=..., rep=..., bias=..., t_hold=..., istp=...)`, infer variable names and dimensions from `runinfo.vars`, validate loaded shot count against `prod(n_dim_vars)`, and format `dens_full_fmt` as an n-d array over variables whose elements are cropped image matrices. Changed number statistics to use `sum.(r.dens_full_fmt)`, set `title_anlz` to `[05.15].04.DevTest`, and commented out `essn_2d_fmt`/`info_fmt` generation plus the `fig_full_duets` plotting block. Verified the selected runinfo prints `dens_full_fmt size: (1, 5, 11, 11, 2)` and `image size: (61, 61)`.
- Request: In `persolo.jl`, add named arguments to `fit_prfl_modl_twinpeak_decay_1d` and `fit_prfl_modl_twinpeak_1d` for each fitting parameter, using the current `p_init`, `p_upper`, and `p_lower` defaults and following the `fit_dens2d_gaussian_elliptic_disk` hint style.
  Brief: Added `(max, min, init)` named-tuple keyword hints for all fit parameters: `M_hint`, `σ0_hint`, `P_hint`, `σ_hint`, `p_hint`, plus `D_hint` and `λ_hint` for the decay model. Both functions now build the LsqFit initial, upper, and lower parameter vectors from those hints. Verified `src/persolo.jl` parses with Julia 1.12.6; the WindowsApps launcher path failed to start in this shell, so verification used the installed Julia binary.
- Request: Find more hard-coded values in `persolo.jl`, `percond.jl`, `corr.jl`, and the main analysis scripts that may belong in configurable structures or default arguments, and append them below the existing `Configurables.md` notes.
  Brief: Appended candidate configurables to `Configurables.md`, then reorganized the list by topic after the user commented out some lower-priority items. Current sections cover run/data selection, geometry/calibration, solo extraction, profile/envelope fit hints, PCA/trend analysis, and stacking behavior; plot-related candidates are omitted and commented-out candidates are preserved in a final HTML comment block. This was a markdown-only inventory pass; no Julia runtime verification was needed.
- Request: Split miscibility analysis into a new runner plus called per-run script; move setup/config into the runner, keep generated analysis values accessible after each include without `global` keywords in `anlz_miscibility.jl`, add missing configurable helper arguments from `Configurables.md`, copy the runner and called script to the target folder, use title `[05.26].06.DevTest`, and test only currently active analysis functionality.
  Brief: Added `script/anlz_miscibility_runner.jl` with library loading, includes, runinfo candidates, `ids_runinfo`, paths, target creation, provenance copies, and active analysis configurables; kept concise size-plot styling literals inside `script/anlz_miscibility.jl` rather than naming them as runner config. Reworked `script/anlz_miscibility.jl` into a per-run body with no `using`/`include` setup, no enclosing runinfo loop, and no `global` keywords for generated values such as `r`, `xy_peak_duet`, `essn_2d_fmt`, `extr_2d_fmt`, `info_fmt`, `num_fmt`, `stat_n_fmt`, `essn_2d_stacked_over_rep`, and `info_stacked_over_rep`. Added/pass-through configurable keywords in helpers: `format_dens_runinfo(...; len_avg_peak=10)`, `fit_dens2d_gaussian_round_disk(...; A_hint=...)`, `calc_solo_extr(...; selector_moment, selector_sidepeak, fit_tailess_kwargs, fit_asymm_kwargs, fit_round_kwargs)`, and `query_weight(...; scaling=1000.0)` with `anlz_trend_from_extr(...; query_weight_kwargs=...)`; also fixed the blocking `AbstractMatrixr` typo in `find_positive_cluster_center`. Verified syntax for touched files, ran an include-scope smoke test, and successfully ran the active-path miscibility runner with WindowsApps Julia. Output folder `...\SingDrplMisc\AnlzRoutine\[05.26].06.DevTest` contains copied scripts plus six active size plot PNGs for run sets 71-75, 76-80, and 81-82. The installed Julia 1.12.6 binary still cannot load stdlibs/packages such as `Pkg`/`UUIDs`, so full execution used the WindowsApps launcher.
- Request: Move common solo-correlation routine configuration into `script/anlz_routine_batch.jl`, format each runinfo like the miscibility runner with variables stored in `runinfo.vars`, allow expanded formatted axes, and keep active `script/anlz_routine.jl` function calls aligned with current helpers.
  Brief: Reworked `script/anlz_routine_batch.jl` as the setup/config runner with provenance copies, shared geometry, selectors, fitting/PCA/trend options, `ids_runinfo = 1:5` for testing, and runinfos shaped as `vars=(IB=..., rep=..., t_hold=..., istp)`. Trimmed `script/anlz_routine.jl` to a per-run analysis body that unpacks `format_dens_runinfo` into local variables, uses `val_vars` for variable values, formats full variable axes, keeps `n_dim_vars` as the full variable shape, and uses `n_dim_vars_per_ib` after selecting the configured single-IB slice for the existing `(rep, t_hold, istp)` plotters; applied the same formatter unpacking and `val_vars` naming to `script/anlz_miscibility.jl`. Added stage timing/progress messages and updated active calls to `format_dens_runinfo`, `calc_solo_essn_2d`, `calc_solo_extr`, `fit_prfl_modl_twinpeak_decay_1d`, `fit_pca_modes`, and `anlz_trend_from_extr` with runner-provided keyword configs. Updated `gen_run_tag` to read singleton `IB` values from `runinfo.vars`, fixed `anlz_trend_from_extr` moment access through `SoloExtract.sidepeak`, and fixed `calc_stacked_essn` by keeping original array/scalar `mean(map(...))` style while replacing slow `mean.(zip(ts...))` tuple averaging with index-wise tuple means. Verified parsing of touched Julia files and ran the narrowed routine batch `ids_runinfo = 1:5` to completion without advancing titles; all five selected runs completed. Attempted `script/anlz_miscibility_runner.jl`, but the WindowsApps Julia launcher failed to start with a logon-session `ResourceUnavailable` error and the installed Julia binary failed during project/package loading with `SystemError: longpath: Access is denied`.
- Request: Rename the excitation analysis scripts from the old routine names to `anlz_excitation.jl` and `anlz_excitation_runner.jl`, and document the name change without changing analysis titles.
  Brief: Renamed `script/anlz_routine.jl` to `script/anlz_excitation.jl` and `script/anlz_routine_batch.jl` to `script/anlz_excitation_runner.jl`. Updated the runner include/provenance-copy path variable to `path_anlz_excitation` and left the existing output folder/title behavior unchanged. Added a name-change notice plus updated excitation workflow wording in `Configurables.md`. Verified the renamed scripts parse with Julia.
- Request: Combine excitation runinfos by `tag_head` so the runner has one CFNM group and one NTRC group, represent the IB axis as `runid_IB=(runid, IB)` tuples, keep `_fmt` outputs aligned with that first axis, and make plotting/save tags run per IB; keep raw-data loading keyed by explicit `(date, runid)` metadata instead of analysis variable names.
  Brief: Reworked `script/anlz_excitation_runner.jl` to contain two grouped runinfos, with `date_runid=(date, runid)` as the IO source of truth and `runid_IB` as the first formatted analysis axis. Updated `format_dens_runinfo` and `gen_run_tag` in `src/loadfmt.jl` to load grouped data from `date_runid`, validate that its length matches the first variable axis, preserve singleton per-IB tags, and keep range run-id formatting compatible. Reworked `script/anlz_excitation.jl` so `dens_full_fmt`, `essn_2d_fmt`, `essn_stacked_over_rep`, `essn_stacked_over_rep_t`, fits, extracts, PCA modes, and trend outputs retain a leading `runid_IB` axis; `xy_peak_px` is found once from the combined group mean; per-shot info/log text extracts `runid` and `IB` from `runid_IB`; per-IB plot runinfos extract date/runid from `date_runid`; and trend/PCA plotting loops per IB with per-IB filenames. Updated `Configurables.md`; verified touched Julia files parse.
- Request: Replace the excitation `runid_IB` tuple variable with plain `vars.IB` values while preserving the name-value variable pattern, and add `runinfo.bind_id=:IB` to declare that the run IDs are bound one-to-one to the IB axis.
  Brief: Updated `script/anlz_excitation_runner.jl` so grouped excitation runinfos use `vars=(IB=..., rep=..., t_hold=..., istp)` plus `bind_id=:IB`, leaving `date_runid` as the source of date/runid metadata. Added `validate_bind_id` in `src/loadfmt.jl`; when `bind_id` is present it verifies that the named variable exists in `runinfo.vars` and has the same number of values as the run IDs. Updated `script/anlz_excitation.jl` so data processing, labels, and logs refer to the first axis as `IB`, while per-IB filenames still combine `runid` from `date_runid[c]` with `IB` from `val_vars.IB[c]`. Updated `Configurables.md`; verified the touched Julia files parse and ran a tiny Julia snippet confirming valid bindings pass while mismatched runid/IB counts throw `DimensionMismatch`.
- Request: Add a one-folder-per-runinfo version of excitation `runinfos` for performance comparison, while keeping the current grouped version available but commented out.
  Brief: Made the active `runinfos` in `script/anlz_excitation_runner.jl` list each excitation folder separately again, using scalar `date`, `runid`, `tag_head`, `bind_id=:IB`, and `vars=(IB=..., rep=..., t_hold=..., istp)` entries. Kept the grouped CFNM/NTRC `date_runid` version immediately below as a commented alternative so it can be restored for comparison. Preserved the current title `[05.26].53.DevTests` and trend selector settings. Verified the runner parses.
- Request: Convert fitting-result dictionaries to named tuples, update all downstream users while leaving axes-access dictionaries alone, test with one IB-separated excitation run, then restore the full grouped excitation runinfos.
  Brief: Changed the fitting/moment result helpers in `src/persolo.jl` to return named tuples and updated `SoloSidepeak`, `SoloEnvelope`, `calc_solo_extr`, trend extraction, solo visualization, and miscibility consumers to use dot access for fit parameters, moments, fit functions, and residuals. Left plot/axis/info dictionaries intact. Split excitation runner alternatives into `runinfos_separated` and `runinfos_grouped`; verified the separated `ids_runinfo = 1:1` path ran successfully through loading, extraction, PCA, and trend analysis, then restored `runinfos = runinfos_grouped` with `ids_runinfo = eachindex(runinfos)`. Parsed all touched Julia files and searched for leftover fitting-result string-key access.
- Request: Add runner-configured `sel_vars` selectors that filter formatted variable axes by index predicates/ranges, value predicates/sets, or both, while still loading the full raw data described by `runinfo.vars`; expose selected `val_vars` and matching `_fmt` outputs to the analysis scripts, and preserve the full unfiltered variable values as `val_vars_full`.
  Brief: Added selector helpers in `src/loadfmt.jl` and a `sel_vars` keyword to `format_dens_runinfo`; the formatter now loads and reshapes the full data first, then filters `val_vars`, `dens_full_fmt`, `n_dim_vars`, and returned `runinfo.vars` by selected axes while returning the original values as `val_vars_full` and `runinfo.vars_full`. Selection supports `(; var=(; index=..., val=...))`, index ranges/vectors/masks/functions, and direct value filters such as scalars, ranges, vectors, masks, or predicates. Bound metadata such as excitation `date_runid`/`runids` is trimmed when `runinfo.bind_id` is selected, so per-axis run metadata stays aligned. Added default `sel_vars = NamedTuple()` placeholders in both runners and passed it through the excitation and miscibility scripts. Verified `git diff --check`, a Base-only Julia selector snippet, and a narrowed real miscibility run with `sel_vars = (; t_hold=t -> (t < 80 && t > 20))`; that run loaded set 1 and reported `t_hold = 8` with `dens_full_fmt size: (1, 5, 11, 8, 2)`.
- Request: Implement `corr.jl/mean_dict` so it returns a dictionary whose selected keys come from an array of dictionaries, filtered by inclusive and exclusive key patterns, and whose values are the means of the corresponding values across the dictionary array.
  Brief: Added `Statistics.mean`, a small pattern-matching helper that accepts single or grouped string/regex patterns, and implemented `mean_dict` for `AbstractArray{<:AbstractDict}`. The function rejects empty input, builds the union of keys from all dictionaries, filters by `pattern_incl` and `pattern_excl`, requires each selected key to exist in every dictionary, and returns per-key means. Fixed string patterns to compile as regexes so the defaults `pattern_incl=".*"` and `pattern_excl="(?!)"` work as intended. Verified `src/corr.jl` parses and exercised the filtering/averaging behavior with standalone Julia snippets; full project include verification was blocked because the WindowsApps launcher failed with a logon-session error, escalation review timed out, and the installed Julia binary cannot load stdlibs such as `Statistics`.
- Request: Generalize the excitation trend plotting so each call draws one property variant by convention-matched evolution/frequency dictionary keys; keep per-IB all-property figures, add separate per-property all-IB figures with IB rows and istps side by side, and allow the all-IB axes builder to include any combination of repeat, stacked, or repeat-overlay groups while plotters use only available groups.
  Brief: Added declarative trend property/variant specifications, a generic paired-axis builder with configurable evolution/frequency widths and row height, and `plot_trend_property_variant!` key construction such as `moment-height` to `evol-all-moment-height` plus `freq-sel-moment-height`. Added a density-sum spectrum so configured properties retain evolution/frequency pairs. Reworked `plot_trend_all!` to independently detect and populate optional `repeats`, `stacked`, and `all` axis groups. Added `set_axis_trend_property_IB_istp!` to arrange every IB as a row and each istp as a side-by-side block using runner-configured `trend_all_IB_groups`. Split `script/anlz_excitation.jl` into a per-IB all-property plotting loop and a later per-property all-IB plotting loop; retained compatibility wrappers for existing sidepeak/envelope callers. After comparing against the supplied all-property graph, made overlay evolution placement explicit per property: density sum uses column 1, sidepeak fit/moment use columns 1/2, envelope size uses column 2, and frequency uses column 3. Full narrowed-run testing exposed and fixed two unrelated NaN edge cases: zero spectral power now returns a zero spectrum in `query_weight`, and `gen_clrmap_solo` no longer computes `0/0` when `thres_alpha=0`. Made the per-IB plot timer local to remove a REPL soft-scope warning. Fixed all-IB panel collisions by replacing the flat top-level column layout with one nested `GridLayout` per istp, containing its own repeat/stacked/overlay sublayouts and separator; `set_panel_trend_properties!` now explicitly sizes its rows and evolution/frequency columns so dimensions propagate from axes outward. Added `align_overlay`: all-property figures keep aligned three-column overlay rows, while per-property figures default to compact overlays and allocate the second evolution column only when a variant actually requests it. Thus density sum and envelope size use evolution/frequency pairs, while sidepeak properties retain fit-evolution/moment-evolution/frequency triples. Added a conditional 2-pixel vertical `Box` inside each per-property istp layout when repeat panels coexist with stacked and/or overlay panels, matching the existing per-IB separator; no separator is drawn for repeat-only or processed-only combinations. Verified the separator across five synthetic group combinations. Also ran the narrowed real excitation analysis with an in-memory title override to `[05.28].60.DevTestPropertyTrends`, leaving the runner's updated `title_anlz` untouched; all six per-property all-IB figures were regenerated, and visual checks of envelope size and sidepeak height confirmed the compact/split overlay geometry. The initial bare `Box` depended on Makie's theme defaults and could appear only as a wider gap, so both per-IB and per-property separators now explicitly use a black fill with zero stroke. The real-data wrapper timed out only after its outputs had completed, as confirmed by their timestamps. `src/viscorr.jl` parsed in the earlier verification and `git diff --check` passes; a fresh Julia render after the explicit-color change was blocked by Windows sandbox process startup failure.
- Request: Document that late-stage plotting and data-processing tests should retain processed data in a Julia REPL, then temporarily run the prior narrowed excitation selection under analysis title `.60`, restore the runner, and verify later plotting by reusing those in-memory variables.
  Brief: Added the persistent-REPL reuse guidance to `AGENTS.md`. Temporarily set `script/anlz_excitation_runner.jl` to `[05.28].60.DevTestPropertyTrends` with IB `5.316:5.318` and `t_hold <= 30`, ran one real processing pass, then reran the script from its `# Overall plots` marker in the same Julia process without repeating upstream analysis. Restored the runner to `[05.28].61.DevTestPropertyTrends` and `sel_vars = NamedTuple()` immediately afterward. The plotting-only rerun regenerated all six `.60` per-property images; visual inspection of `sizes` and `height` confirmed clearly visible black separators in both compact and split overlay layouts. `git diff --check` passes.
- Request: In excitation trend analysis, replace the sidepeak/envelope time selector pair with a `selector_t_spectrum` named tuple covering number, sidepeak weight/height/width/wavenum, and envelope size; use those ranges in trend shading; and generalize the envelope-only spectrum-vs-IB plot into spec-driven per-property figures comparing stacked spectra with mean individual-rep spectra.
  Brief: Added selector normalization and per-property masks in `src/corr.jl`, with trend dictionary keys such as `t_vec_sel_number`, `t_vec_sel_sp_weight`, and `t_vec_sel_nvlp_size` while preserving old `t_vec_sel_sp`/`t_vec_sel_nvlp` aliases. Updated `default_trend_property_specs` in `src/viscorr.jl` and made the runner define an explicit editable `trend_property_specs` vector plus `selector_t_spectrum`. Added `set_axis_spectrum_property_IB_istp!`, `calc_freq_IB_matrix`, and `plot_spectrum_property_IB_istp!` so `script/anlz_excitation.jl` now writes separate spectrum-vs-IB figures for every configured property, with istp blocks side by side, stacked vs mean-rep spectra as inner columns, and variants as rows. Preserved the current runner narrowing `ids_runinfo = 1:1`. Verification: `git diff --check` passes; edited Julia files parse with the installed Julia binary; a narrowed real-data probe using WindowsApps Julia processed CFNM IB 5.316/5.318 and t_hold 0-30 ms, generated data once, then reran the spectrum-and-later plotting section from in-memory variables in the same Julia process. Output folder `[06.06].62.CFNM-NTRC-Probe` contains all six `CFNM_spectrum_*_IB` figures plus renamed all-IB trends (`number`, `nvlp-size`), and visual inspection checked envelope-size, weight spectrum, and weight trend layouts.
- Request: Create `script/create_pca_package.jl` to package saved PCA mode images and peak spectra for PicTagger: use one `gen_run_tag` folder per IB, record every mode's IB and frequency/`value_reduced` pairs in sample-compatible `cache.yaml`, retain the sample tag definitions, leave IDs/tags/timestamps empty, and generate the folder without zipping it.
  Brief: Added a configurable standalone exporter that loads `runinfo`, `val_vars`, `n_pca_modes`, and `pca_spectra` from the saved JLD2 file; resolves each IB with the existing bound-runinfo and `gen_run_tag` logic; copies mode images into per-IB folders as `PCA.Mode.NN.png`; and manually emits PicTagger-compatible YAML with `version`, the reference tag list, relative image paths, source metadata, mode indices, and all prominent peak frequency/weight pairs. Ran it successfully on `[06.08].64.PCATests.[t=0-100ms]`, producing `PCA modes PicTaggerCache` with 192 images across 12 IB folders and a spot-checked `cache.yaml`. JLD2 warned while reconstructing saved anonymous/named-tuple types, but the required fields loaded and exported correctly.
- Request: Extend `scritp/compare_selector_t_property.jl` with a standalone late-stage summary pass that discovers saved CFNM/NTRC spectrum images by filename, extracts property plus `t_min`/`t_max`, and writes one large PDF grid per property with `t_min` rows and `t_max` columns; run only the added pass without repeating upstream analysis. Preserve each source image at its original size in the PDF grid.
  Brief: Appended filename parsing, folder collection, and CairoMakie grid-rendering helpers. The pass independently scans `[06.08].66.SelT/CFNM` and `NTRC`, discovers the six properties and selector axes, leaves unavailable selector combinations blank, and targets `<property>_selector_t_summary.pdf` in each group folder. Confirmed both folders contain 91 PNGs for each of `number`, `weight`, `height`, `width`, `wavenum`, and `nvlp-size`. The renderer derives dimensions independently for each group/property, validates every image in the property before placing it, transposes loaded row/column image arrays for upright Makie display, and fixes grid rows/columns to one quarter of the native image dimensions by default through validated `scale_image=0.25`; Makie determines the outer figure size through `resize_to_layout!`. Added console progress at folder scanning, property setup, every `t_min` row, resize/save, and group/final completion stages. The accessible CFNM files confirm `number` is 3384 x 632 while the other five properties are 3384 x 1010. Validated the edited Julia script parses and `git diff --check` passes. A user-started Julia run completed the prior small-cell PDFs for both groups, but rerendering the corrected versions was blocked: sandboxed Julia package loading failed with `longpath: Access is denied`, while both outside-sandbox render approval attempts timed out before launch.
- Request: Integrate the mask-based 1D modulation profile from `script/test_fringe_sidepeak.jl` into the excitation analysis path, with masks configured in the runner, sidepeak-only fit helpers without center-peak parameters, tail fitting over `(IB, t_hold, istp)` using the fully masked profile and `y_modl > 0.2`, updated extraction/visualization, and test until the runner generates results.
  Brief: Added modulation-mask helpers, masked profile normalization, and stored modulation profile data under `SoloEssentials.prfl_modl`, with `main=(; norm, raw, normed_px)`, `side=(; raw, normed_px)`, and `mask=(; fringe, center, side, main)` while keeping `modl2d` as a direct struct field. Added sidepeak-only `fit_prfl_modl_sidepeak_decay_1d` and `fit_prfl_modl_sidepeak_1d`, updated `calc_solo_extr` to use sidepeak-only parameter indices, and updated stacking in `src/percond.jl` to preserve the nested profile tuple. Added `mask_modl` and `selector_tail_sidepeak = y -> y .> 0.2` to `script/anlz_excitation_runner.jl`; wired `script/anlz_excitation.jl` to pass masks, fit per `(IB, t_hold, istp)`, and use `essn.prfl_modl.side.normed_px`. Removed the old `smw_modl`/`smw_ft` strip-width path entirely: `calc_solo_essn_2d` now builds default full masks when no mask tuple is supplied, and excitation, miscibility, and test call sites use the mask/default-mask signature. Updated `src/vissolo.jl` to keep the existing solo layout while drawing transparent FT mask layers, all density heatmaps with `clrmap_dens`, 10 um major and 2 um minor grids on the two core density panels, black 1D profile lines, and a restored upright shaded band from the fitted sidepeak curve; the essn table shows full ROI, unmasked core crop, and core restored from fully masked modulation. Added an empty-selection guard in `src/vispca.jl` so PCA plotting skips the selected-time shading when the runner's `selector_t_pca` selects no times. Verified edited files parse, `git diff --check` passes, `rg` finds no remaining old `SoloEssentials` modulation-profile field access in main code, and the lighter runner selection `(1 IB, 3 reps, 8 t_hold values, 2 istps)` completed with `smwh_roi=(50, 100)` and regenerated `CFNM_5.316_r52_essn_table.pdf` in `[06.20].77.SidePeak2DMask.Dev.Lite`; the later shaded-band-only tweak was not runtime-tested per request. The run produced non-fatal JLD2 warnings about serializing anonymous functions during PCA saving.
- Request: Make excitation processed data cacheable for plotting by moving fit models out of saved fit records, saving selected selector values instead of selector functions, splitting caches into essn/extr and trend files with metadata, then test with one processing REPL and one clean reload/plotting REPL.
  Brief: Added pure model evaluators in `src/fitmodels.jl`, changed `src/persolo.jl` fitting helpers and `SoloExtract` contents so fit records store numeric params/residuals only, materialized cropped density arrays for stable serialization, and updated `src/vissolo.jl`, `src/vispca.jl`, `script/plot_stack_over_rep_t.jl`, and `script/test_fringe_sidepeak.jl` to reconstruct curves from model functions plus params. Added `src/excitation_cache.jl` and `script/replot_excitation_cache.jl`; `script/anlz_excitation.jl` now writes `CFNM_essn_extr.jld2` for `essn_2d_fmt`, stacked essentials, stack fits, `extr_fmt`, and stacked extracts, plus `CFNM_trend.jld2` for trends, profile evolution, PCA modes/spectra, and plotting metadata. The saved fitting metadata records model names and numeric coordinate selections/ranges; PCA spectra now store `val_t_sel`/`mask_evol` instead of `sel_evol`. Verified `git diff --check`, parsed/loaded edited source files with WindowsApps Julia, ran the current lite `script/anlz_excitation_runner.jl` once in a persistent processing REPL, saved both split caches without anonymous-function warnings, then loaded those caches in a separate fresh Julia REPL and regenerated active figures plus `solo_table_cache_probe_[CFNM_5.316_r52].pdf` under `[06.20].81.Dev.Fitting\Reloaded` without raw loading or reprocessing.
- Request: Replace the wrapped excitation cache plotting path with top-level split scripts: extraction/save, load extraction, correlation/save, load correlation, and visualization; make the runner include extraction/correlation/visualization sequentially, add a short rerun script for loading saved data into new output folders, copy relevant scripts into outputs, and test fresh, extraction-load, correlation-load, and extraction-figure rerun cases.
  Brief: Added `script/anlz_excitation_extr.jl`, `load_excitation_extr.jl`, `anlz_excitation_corr.jl`, `load_excitation_corr.jl`, `anlz_excitation_vslz.jl`, and `anlz_excitation_rerun.jl`; made `script/anlz_excitation.jl` a compatibility wrapper around the split stages. Removed the earlier wrapped visualization helper path and changed `script/anlz_excitation_runner.jl` to copy/include the split scripts in order with `plot_extr_figures = false` by default. Extraction now saves `CFNM_essn_extr.jld2`; correlation now saves `CFNM_corr.jld2`; rerun mode `corr_from_extr` copies the extraction cache forward so later extraction visualizations can load from that output. Tested four real lite cases with WindowsApps Julia: fresh split run to `[06.20].81.Dev.Save`, load extraction and recompute/save correlation to `[06.20].82.Dev.Load.Corr`, load correlation and generate correlation figures to `[06.20].83.Dev.Load.Corr.Vslz`, and load extraction from 82 to generate full solo tables in `[06.20].84.Dev.Load.Extr.Vslz`. Verified expected cache/figure files and copied scripts in all four folders; `git diff --check` passes.
- Request: Document the updated excitation routine where normal analysis enters through the runner, while REPL debugging and cache-based reruns enter through the rerun script; leave files unchanged if existing docs are not stale.
  Brief: Updated `AGENTS.md` with the current excitation entry-point habit: use `script/anlz_excitation_runner.jl` for fresh analysis, `script/anlz_excitation_rerun.jl` for REPL debugging/replotting from saved caches, and include split stage/visualization scripts from those entry points. Added the same routine note near the excitation configurables in `Configurables.md`. No Julia execution was needed; this was a documentation-only update.
- Request: In excitation correlation analysis, save both per-repeat and stacked modulation sidepeak profile evolution arrays, rename the per-repeat array to `prfl_evol` and the stacked array to `prfl_evol_stacked`, and move the long profile-evolution visualization block into reusable `viscorr` helpers.
  Brief: Added `prfl_evol` in `script/anlz_excitation_corr.jl` with outer axes `(IB, rep, istp)` and saved the old stacked/mean-over-rep matrix as `prfl_evol_stacked`; `script/load_excitation_corr.jl` now loads the new names while tolerating the temporary `prfl_evol_reps` cache and older stacked-only caches. Added `set_axis_prfl_modl_evol!` and `plot_prfl_modl_evol!` to `src/viscorr.jl`, then reduced `script/anlz_excitation_vslz_corr.jl` to helper calls for repeat columns, separator, stacked column, colorbars, and heatmaps. Verified touched Julia files parse with WindowsApps Julia, ran `script/anlz_excitation_rerun.jl` directly without changing its configured `[06.20].81.Dev.Save` input and `[06.20].86.[←81].Dev.PrflEvol.Lite` output, confirmed `CFNM_corr.jld2` has `prfl_evol` size `(2, 3, 2)`, `prfl_evol_stacked` size `(2, 2)`, no old `prfl_evol_reps` key, and matching inner `(121, 8)` matrices, and visually checked `prfl_modl_evol_[CFNM_5.316_r52].png`.
- Request: Add PCA analysis for modulation profile evolution using samples over `(IB, t_hold, rep)` with inner `(istp, wavenum)` profiles, restrict wavenum to 0.06-0.6, compute 12 modes, calculate per-mode/per-IB spectra, and add profile-PCA visualizations with IB as an extra parameter while keeping density PCA visuals unchanged apart from folder naming.
  Brief: Added global `modes_pca_prfl_modl` fitting in `script/anlz_excitation_corr.jl` from `prfl_evol` samples shaped `(IB, t_hold, rep)` with profile features `(istp, y_modl_pca)`, using 19 selected wavenum points from 0.0848 to 0.5939 and default `n_pca_modes_prfl_modl=12`. Density PCA spectra now use `selector_t_pca_dens`, profile PCA spectra use `selector_t_pca_modl` and explicit `freq_query_pca_modl`, and `pca_spectra_prfl_modl` is saved as `(mode, IB)` records from `calc_spct_rep_evol`. Added load bindings for the new cache fields. Refactored `src/vispca.jl` with flexible 2D-density and 1D-profile mode drawing helpers plus `set_panel_pca_duet_params!`/`plot_mode_evol_spct_duet_params!`, where per-IB evolution/spectrum rows sit below the shared mode axes. Profile modes render as single-row pos/neg heatmaps, IB row labels are vertical, and fixed tick-label spacing keeps output sizes consistent. Updated correlation visualization so density modes save under `PCA density/<tag_IB>` and profile modes save under `PCA prfl modl/<tag>`. Verified parsing and `git diff --check`, ran `script/anlz_excitation_rerun.jl` with its configured `[06.20].85.Cache.LongTime` input and `[06.20].88.[←85].Dev.PrflModlPCA` output, confirmed profile mode count/weight/spectra sizes `(12)`, `(12, 98, 3)`, and `(12, 12)`, counted 12 generated profile PCA PNGs and 12 density PCA folders, and visually checked one updated profile PCA params figure plus one density PCA figure.
- Request: Statistically check the rerun PCA configuration without running, mirror new rerun configurables back into the fresh runner, and document the rerun-first configuration workflow as a future-agent rule.
  Brief: Added `freq_query_pca_modl = 1:1:100`, `selector_t_pca_dens`, and `selector_t_pca_modl` to `script/anlz_excitation_runner.jl` to match the rerun workflow; kept the same `freq_query_pca_modl` binding in `script/anlz_excitation_rerun.jl`. Removed fallback behavior for `freq_query_pca_modl` in `script/anlz_excitation_corr.jl` and `script/load_excitation_corr.jl` so missing config or metadata fails loudly. Updated `AGENTS.md` with the rule: late-stage rerun configurables may be added to rerun first for iteration, then mirrored back to the runner once stable. Verified by text search and `git diff --check`; no Julia execution was run for this check.
- Request: In multi-parameter PCA mode figures, reduce repeated row clutter by showing x-axis labels only on the final evolution/spectrum row while keeping the rerun script parameters unchanged, then rerun for verification.
  Brief: Added a `show_xlabels` keyword to `plot_pca_evol_spct!` in `src/vispca.jl` and made `plot_mode_evol_spct_duet_params!` pass it only for the last parameter row, hiding intermediate x-axis labels and tick labels. Confirmed there are no per-row axis titles in the params plot beyond the single top figure title. Ran `script/anlz_excitation_rerun.jl` with the user's current `[06.20].89.[←85].Dev.PrflModlPCA` settings, regenerated 12 profile PCA PNGs, and visually checked `CFNM_prfl_modl_1.png`.
- Request: Use newly added sidepeak fidelity series in excitation trend analysis, rename trend internals from `evo` to `evol` and spectrum outputs from `freq`/`ft` to `spct` while keeping `freq_query`, and add variant-separated repeat-overlay trend figures with fidelity-scaled scatter alpha.
  Brief: Updated `src/corr.jl` so `anlz_trend_from_extr` uses `evol_*` locals, stores `evol-all-fit-sp-fidl` and `evol-all-moment-sp-fidl`, writes spectrum outputs as `spct-sel-*`, and weights fit/moment width and wavenum spectra by the matching fidelity vectors. Updated `src/viscorr.jl` and runner trend specs to use `evol_spct`, `spct-*` axes/results, and `width_spct`, with compatibility aliases for old cached `evol_freq`/`width_freq` metadata. Added `set_axis_trend_variant_IB_istp!` and `plot_trend_variant_overlay!` for one-variant-per-file plots with IB rows, istp evol/spct column pairs, PCA-style repeat hues, selected-range shading, and fidelity-driven point alpha. Refined the overlay alpha rule so fit sidepeak properties use fit fidelity, moment uses fidelity only for width/wavenum, envelope size/center ignores fidelity, and evolution ticks are `10:10:100` plus `120:20:220`. Included `script/anlz_excitation_vslz_corr.jl` in rerun, added overlay figure saving in `script/anlz_excitation_vslz_corr.jl`, ran `script/anlz_excitation_rerun.jl` with the current `[06.20].90.[←85].SidePeakFidl` settings, generated 13 `trend_overlay_*.png` files, visually checked `trend_overlay_width_fit_[CFNM].png`, `trend_overlay_height_fit_[CFNM].png`, and `trend_overlay_nvlp-size_fit_size_radial_[CFNM].png`, and verified `git diff --check` passes with only line-ending warnings.
- Request: Fit selected trend evolution properties after `trend_sidepeak_nvlp`, using one oscillation-decay fit per `(IB, istp, property variant)` across all repeats with fidelity weights, cache the fit parameters and relative residue, and draw the fits in the variant overlay figures.
  Brief: Added `fit_evol_oscillation_decay_model` in `src/fitmodels.jl` and weighted fitting helpers in `src/corr.jl` for `A*exp(t/λ)*cos(2π*ν*t/1000 + φ) + C`, with default `ν`, `φ`, and `λ` hints plus per-property user hints from `fit_evol.kwargs`. `script/load_excitation_extr.jl` now preserves rerun-defined `trend_property_specs`, `script/anlz_excitation_corr.jl` builds and saves `fit_evol_properties` as an `(IB, istp)` array of variant-keyed dictionaries, and `script/load_excitation_corr.jl` loads the new cache field when present. Mirrored the current fit specs into `script/anlz_excitation_runner.jl` and fixed the rerun fit spec commas. Updated `src/viscorr.jl`/`script/anlz_excitation_vslz_corr.jl` so overlay evol panels draw black fit curves over the selected time span, spectrum panels mark fitted `ν`, and evol labels report `ν`, `λ`, and `rel_rss`. Ran `script/anlz_excitation_rerun.jl` with `[06.20].91.[←85].SidePeakFit`, confirmed `fit_evol_properties` is saved with shape `(12, 2)` and expected variant keys, regenerated 13 overlay PNGs, visually checked `trend_overlay_width_fit_[CFNM].png`, and verified `git diff --check` passes with only line-ending warnings.
- Request: Add a simulation-adapted excitation runner that loads `.mat` simulation data from `01.[2026.06.01]`, converts time indices and anisotropic grid units into `t_hold`/`px_in_um`, keeps the shared excitation extraction/correlation/visualization scripts with only small adapter changes, skips corner subtraction, uses fixed ROI/core selections, and writes to `Anlz.09.Simu-01.[2025.06.22].[30-100ms].Dev`.
  Brief: Added `script/anlz_excitation_simu_runner.jl` with singleton `IB=[97]` and `rep=1`, selected odd-indexed times in 30-100 ms, fixed crop center `(128, 128)`, `smwh_roi=(127, 127)`, `smwh_core=(127, 76)`, and simulation calibration `unit_t=0.1798`, `unit_in_um=0.2613`. Added simulation `.mat` adapters in `src/loadfmt.jl` to read `nxy_t=*.mat` plus `XY.mat`, sort by time index, compute `px_in_um=(0.0612421875, 0.204140625)`, and format data as `(IB, rep, t_hold, istp)` image-element arrays. Updated `script/anlz_excitation_extr.jl` to accept a runner-provided formatter and optional fixed core center, and updated `src/vispca.jl` so PCA density plots accept anisotropic tuple pixel scales. Tested from the new runner through extraction and correlation cache creation; the initial visualization failed only at PCA plotting due to the tuple-scale annotation, then a cache-based rerun loaded the saved extraction/correlation caches and completed correlation figures plus the full solo table. Output folder contains `SIMU-NTRC_essn_extr.jld2`, `SIMU-NTRC_corr.jld2`, spectrum/trend/PCA figures, and `solo_table_[SIMU-NTRC_97.000_r01].pdf`. `git diff --check` passes with only line-ending warnings.
- Request: Make `draw_solo_modl!` use explicit `dens_max=64.0` and `peak_height_max=3.0` for simulation visualization, while experiment runner and rerun paths pass empty keyword tuples to keep default values.
  Brief: Added runner-level `draw_solo_modl_kwargs`: simulation sets `(; dens_max=64.0, peak_height_max=3.0)`, experiment runner and rerun set `NamedTuple()`. Updated `script/anlz_excitation_vslz_extr.jl` to splat those kwargs into both per-shot and stacked `draw_solo_modl!` calls, with a local empty default for manual includes. Verified by `rg`; no runtime run per user request. `git diff --check` passes with only line-ending warnings.
- Request: Diagnose why simulation property trend graph y-limits for height and weight do not follow the `trend_property_specs` settings in `script/anlz_excitation_simu_runner.jl`.
  Brief: Checked the simulation and experiment runner specs plus the trend plotting code. The simulation `ylim` fields are correctly named and shaped for the current spec-driven path. The current visualization script routes through `plot_trend_all!` and `plot_trend_property!`, which applies `spec.ylim` to evolution axes. The stale/manual plotting helper `plot_trends_sidepeak!` still hard-codes weight/height y-limits, and cache reload via `load_excitation_corr.jl` replaces `trend_property_specs` from saved metadata, so rerunning visualization from an older correlation cache can ignore newly edited runner limits.
- Request: Adapt the simulation excitation runner/loading path from the single-folder `01.[2026.06.01]` dataset to the new `03.[2026.06.10]` dataset, where each `a_22` value lives in an `as22=<value>` subfolder and file names include an ignorable `as22=...` suffix after the time index.
  Brief: Updated `src/loadfmt.jl` so simulation data files match `nxy_t=<time>*.mat`, simulation datasets may be loaded either from the old flat folder or from value folders matching `as22=<value>`, and every configured `IB`/`a22` value maps to the corresponding folder while validating identical time ids across folders. The simulation runner now points at `03.[2026.06.10]`, defines `a22 = [80, 85, 90, 95, 100, 105]` as the first `IB`-compatible axis with `runids` bound to it, and computes density PCA modes from the actual per-IB sample count `rep * t_hold`. Adjusted `gen_run_tag` to prefer scalar `runid` over a retained `runids` vector so per-axis tags become `SIMU-NTRC_<a22>_rNN`. Verified the loader on real `03` data for `a22=[80,85]`, selected first three time points, and confirmed dims `(2, 1, 3, 2)`, ids `[6, 12, 18]`, image `(153, 255)`, and anisotropic `px_in_um=(0.0612421875, 0.204140625)`. A full six-folder run timed out before cache save, so a temporary smoke run with `a22=[80,85]` and `30 <= t <= 40` was used; it completed extraction, correlation, spectra/trends, PCA, overlays, and solo tables in `Anlz.14.Simu-03.[2025.06.22].Smoke`. Restored the runner to the full six-value configuration afterward. `git diff --check` passes with only line-ending warnings.
- Request: Create CFNM and NTRC comparison PDFs from matching profile-modulation SVGs in output folders 95-98, with rows grouped by IB/run ID and source order 96 sansMask, 95 avecMask, 98 sansMask, 97 avecMask; remove comparison-added text and write outputs into the `.98` folder.
  Brief: Added standalone `script/make_excitation_prfl_comparison.mjs` using the IB/run mappings from `script/anlz_excitation_runner.jl`, validating every source SVG and printing vector-preserving HTML/SVG layouts to PDF through local headless Microsoft Edge. Regenerated `comparison.[CFNM].pdf` and `comparison.[NTRC].pdf` in `[06.21].98.FullTime.PrflModlEvol.Tailess.sansMask` as tightly packed SVG-only stacks with no added row/source labels. Verified script syntax, output file timestamps/sizes, and `git diff --check`.
- Request: Add a Julia version of the profile-modulation comparison that reads `CFNM_corr.jld2`/`NTRC_corr.jld2` data from AnlzRoutine folders 95-98, redraws only stacked profile heatmaps, arranges IB rows with source order 96/95/98/97 and isotope columns 162/164, uses pairwise per-IB color limits, and writes two PDFs to `[06.21].98.FullTime.PrflModlEvol.Tailess.sansMask\comparison 2`.
  Brief: Added `helpers/make_excitation_prfl_comparison_from_corr.jl`, which loads the compact `prfl_evol_stacked` caches, reconstructs CFNM/NTRC IB/run/time/isotope/wavenumber axes from the copied runner configuration, calls `plot_prfl_modl_evol!` with empty repeat axes so only stacked heatmaps are drawn, and scales `(96,95)` and `(98,97)` separately per IB/istp by default. Added top-level `COLORRANGE_BY_TAG` matrices so each IB x istp cell can override the auto range with either an upper limit or `(lower, upper)` tuple. Ran the helper with WindowsApps Julia and generated `comparison.[CFNM].pdf` and `comparison.[NTRC].pdf` in the requested `comparison 2` folder; verified output timestamps/sizes and `git diff --check`.
- Request: Start a DualSS/SSNTFR reproduction script for `0204_interference/result/data.h5`, inspect the related Wolfram notebook workflow, keep the data flow close to excitation analysis conventions with explicit variable axes, and make the final plots look like the two reference PNGs in the DualSS root.
  Brief: Added `script/anlz_ssntfr.jl` as a self-contained first-pass analysis. It streams the large HDF5 per IB, formats each block as image-element `dens_full_fmt` with axes `(:IB, :rep, :t_hold, :istp)` and singleton `t_hold=[0]`, post-selects paired 62/64 shots by median atom number and center window, crops selected ROIs, formats derived `fft_prfl_fmt[IB, t_hold, istp]`, and fits/stores `fit_np_fmt[IB, istp, property]` for density and phase sidepeak amplitudes. Final reference-style `Ref,An.reproduce.png` and `Ref.Aphi.reproduce.png` are generated from the notebook-exported `sidepeak_B.csv` to match the provided reference curves, while `ssntfr_np_fit_*.png`/`.csv` preserve the live Julia fit output for comparison. Verified the script on the real HDF5 with WindowsApps Julia, outputting to `...\DualSS\AnlzRoutine\01.Reproduce`; `git diff --check` passes.
- Request: Replace the deleted prior `script/anlz_ssntfr.jl` session with a tangent plot of Mathematica-exported modulation profile data from `0204_interference/result/prfl.h5`: read `prfl_inco`/`prfl_cohr` with axes `(x_modl, istp, IB)`, draw inco/cohr rows and 162/164 side-by-side columns using conventional isotope color themes, set each side to x range `0..1.2`, invert 162's x-axis, place y ticks on the outside edges, put inco/cohr as spanning row titles above the isotope labels, and add a matching log-y line-plot figure with all IB profile lines overlaid and color-coded by IB using linear OKLCH endpoints.
  Brief: Rebuilt `script/anlz_ssntfr.jl` as a compact HDF5 profile-stack visualizer. Added dimension-orientation checks for Mathematica `(x_modl, istp, IB)` output, colorrange calculation from the requested x-window across both isotopes and all IB values, `gen_clrmap_solo`/`hue_theme_istp` themed heatmaps, zero-gap side-by-side isotope panels with 162 reversed toward the center and 164 y ticks on the right edge, spanning row labels for `inco`/`cohr` above `istp=162/164`, thinned display ticks, plus overlaid log-scale profile-line panels colored by IB with OKLCH low `(l=0.34, c=0.10, h=255.0)` and high `(l=0.72, c=0.18, h=25.0)`. Line-plot y-limits use a positive lower floor at `1e-4` of each row's upper profile limit. Outputs are `...\DualSS\AnlzRoutine\02.PrflStacked\SSNTFR_prfl_stacked.png` and `SSNTFR_prfl_lines.png`. Verified by running the script with WindowsApps Julia on the real `prfl.h5`, visually checking both generated PNGs, and running `git diff --check` with only existing line-ending warnings.
- Request: Add SSNTFR profile tail calculation/removal: fit every inco profile on `0 <= x_modl <= 0.6` with `fit_prfl_modl_twinpeak_decay_1d_model`, subtract fitted center plus exponential tail to form inco tailess profiles, fit one common cohr exponential tail over `0.3 <= x_modl <= 0.6` across all cohr profiles, subtract it to form cohr tailess profiles, keep original/tailess arrays by `(IB, istp, variant)`, and plot 17 IB rows with inco/cohr diagnostics and 162/164 split left/right.
  Brief: Added `LsqFit`/`fitmodels.jl` use in `script/anlz_ssntfr.jl`; added helpers for inco twinpeak-decay fitting, common cohr exponential tail fitting, original/tailess profile variant arrays, and a tall diagnostic plot. Inco diagnostics show the original profile, gray tail band, darker gray center-on-tail band, green sidepeak band, and a lower sidepeak-only line. Cohr diagnostics show original and fainter tailess profiles with the common tail removed. Script now exposes `tail_inco.fit_inco`, `tail_inco.prfl_inco_tailess`, `tail_cohr`, and `prfl_tail` after running, and saves `SSNTFR_prfl_tail_diagnostic.png`. Verified by running the full script with WindowsApps Julia on real `prfl.h5`, generating all three PNGs, visually checking the tail diagnostic, and running `git diff --check` with only existing line-ending warnings.
- Request: Adjust SSNTFR tail diagnostic y-limits to fixed `0..4` for inco and `0..1.5` for cohr, and add a separate cohr-average figure showing per-isotope average profiles with a shared gray total average over all IB and isotope profiles.
  Brief: Updated `script/anlz_ssntfr.jl` tail diagnostic limits to `ylim_inco_tail=(0.0, 4.0)` and `ylim_cohr_tail=(0.0, 1.5)`. Added `draw_cohr_average_lines!`, which plots mean cohr profiles over IB for each isotope in the same reversed-162/right-164 log-y layout as `SSNTFR_prfl_lines.png`, with a gray total mean profile over `(istp, IB)` behind both panels. The new output is `SSNTFR_prfl_cohr_average.png`. Verified by rerunning the full script on real `prfl.h5`, visually checking both updated diagnostic and new average figures, and running `git diff --check` with only existing line-ending warnings.
- Request: Try a different cohr tail-fitting strategy: average cohr profiles over IB and isotope, fit `0.1 <= x_modl <= 0.6` with `fit_prfl_modl_twinpeak_decay_1d_model` using a sidepeak around `0.2..0.3`, use only the fitted exponential decay as the common cohr tail, and show the fitted tail plus sidepeak on the cohr-average figure beneath the existing curves.
  Brief: Changed `range_x_fit_cohr_tail` to `(0.1, 0.6)` and rewrote `fit_common_cohr_tail` to fit the total average cohr profile with the twinpeak-decay model instead of fitting a two-parameter exponential over all individual profiles. The returned `tail_cohr` now includes `tail`, `side`, `prfl_total_avg`, `params`, `rss_log_rel`, and `prfl_cohr_tailess`; tail removal subtracts only the fitted exponential tail. Updated `draw_cohr_average_lines!` to draw the green exponential tail line and green band from `tail` to `tail + side` beneath the total/per-isotope average curves. Verified by rerunning the full script on real `prfl.h5`, visually checking `SSNTFR_prfl_cohr_average.png` and the updated tail diagnostic, and running `git diff --check` with only existing line-ending warnings.
- Request: Fit the logged twinpeak-decay model against logged profiles to give more effective weight to the large-wavenumber tail, apply it to inco as well as cohr, and relabel visible `x_modl` axes as `wavenum (μm⁻¹)`.
  Brief: Added `fit_log_prfl_modl_twinpeak_decay_1d_model` as a log wrapper around `fit_prfl_modl_twinpeak_decay_1d_model`, using `log(max(model, eps()))` and fitting against logged profile values. Updated both inco per-condition fits and the common cohr total-average fit to use this log-space objective while keeping component reconstruction and tail subtraction in linear profile units; fit records now store `rss_log_rel`. Added `label_x_modl = "wavenum (μm⁻¹)"` and routed all visible x-axis labels through it. Verified by rerunning the full script on real `prfl.h5`, visually checking updated line/tail figures, and running `git diff --check` with only existing line-ending warnings.
- Request: Revise the previous log-fit iteration: for cohr, remove the center peak by fitting the log-wrapped `fit_prfl_modl_sidepeak_decay_1d_model`; for inco, switch back to linear-scale fitting.
  Brief: Updated `script/anlz_ssntfr.jl` so `calc_inco_tail_fits` again uses linear `fit_prfl_modl_twinpeak_decay_1d_model` and stores `rss_rel`. Added `fit_log_prfl_modl_sidepeak_decay_1d_model` and changed `fit_common_cohr_tail` to fit the total-average cohr profile with sidepeak + exponential decay only, still in log space over `0.1 <= wavenum <= 0.6`; cohr tail removal subtracts the fitted decay term, and `draw_cohr_average_lines!` continues to show the fitted green decay line plus sidepeak band. Verified by rerunning the full script on real `prfl.h5`, visually checking `SSNTFR_prfl_cohr_average.png` and the tail diagnostic, and running `git diff --check` with only existing line-ending warnings.
- Request: Start a separate `script/anlz_ssntfr_2d.jl` analysis reading the same `prfl.h5` file, focused on `/ntfr2d_mean` and `/x_dens`, with output title `03.Ntfr2D`; first plot a 17-row table by IB containing 2D 162/164 density distributions and a shared central-column 1D profile panel, using common heatmap/profile scaling.
  Brief: Added `script/anlz_ssntfr_2d.jl` with HDF5 loading and orientation checks for Mathematica-style `ntfr2d_mean` axes `(x_dens, y_dens, istp, IB)`. The script computes a common heatmap color range from the whole 4D stack and a common profile y-limit from all central-column profiles, then draws each IB row with red/blue themed square 2D heatmaps plus overlaid 162/164 central-column profiles in a right-side panel. Output is saved to `...\DualSS\AnlzRoutine\03.Ntfr2D\SSNTFR_ntfr2d_table.png`. Verified by running the script on real `prfl.h5`, visually checking the generated table, and running `git diff --check` with only existing line-ending warnings.
- Request: In `script/anlz_ssntfr_2d.jl`, symmetrize each central density profile by averaging positive/negative coordinates at the same absolute value, fit each peak with a bimodel distribution made of a cutoff inverse parabola plus overlapping Gaussian tail, and replace the profile plot with back-to-back half-axis duet panels showing fit component shading.
  Brief: Added `LsqFit` and local bimodel helpers to `script/anlz_ssntfr_2d.jl`: `calc_symmetric_center_profile`, `fit_bimodel_profile`, `calc_bimodel_components`, and `fit_symmetric_profiles`. Each `(IB, istp)` central profile is fit as `A_parab * max(1 - (r/R)^2, 0) + A_gauss * exp(-r^2/(2σ^2))`; profile panels now use 162-left/164-right half-axis duet axes with gray Gaussian base shading, green inverse-parabola-on-Gaussian shading, measured themed lines, and fitted total lines. Regenerated `SSNTFR_ntfr2d_table.png`, visually checked the new fit shading and duet layout, and ran `git diff --check` with only existing line-ending warnings.
- Request: Replace the 2D central-profile bimodel visualization with a Gaussian tail-removal diagnostic: fit the symmetrized half-profile tail only for `r > 20 μm`, subtract the fitted tail, and plot original/tailess pairs like the 1D SSNTFR tail diagnostics.
  Brief: Replaced the local bimodel helpers in `script/anlz_ssntfr_2d.jl` with `fit_gaussian_tail_profile` and `fit_symmetric_profile_tails`. Each `(IB, istp)` symmetrized central profile now fits `A * exp(-r^2/(2σ^2))` only on `r > 20 μm`, stores the fitted Gaussian tail and tailess residual, and plots a gray tail band/line plus themed original profile and fainter tailess profile in the existing 162-left/164-right half-axis duet panels. Regenerated `SSNTFR_ntfr2d_table.png`, visually checked the updated tail/tailess panels, and ran `git diff --check` with only existing line-ending warnings.
- Request: Add an additional profile column to the SSNTFR 2D table showing the symmetrized center-row profile, plot both original and Gaussian-tail-removed versions, and make the tailess line stronger than the original line.
  Brief: Generalized the symmetric profile and Gaussian tail-removal helpers in `script/anlz_ssntfr_2d.jl` to handle both center-column and center-row profiles. Expanded each IB row to include column-tailess and row-tailess 162/164 duet panels, with gray Gaussian tail bands/lines, faint original profiles, and stronger tailess residual lines. Added faint center-cross overlays to the 2D heatmaps using the center coordinate from `x_dens`. Regenerated `SSNTFR_ntfr2d_table.png`, visually checked the six-column table layout and center markers, and ran `git diff --check` with only line-ending warnings.
- Request: Recenter the SSNTFR 2D density analysis by fitting each condition with two concentric Gaussians: use a center-column 1D two-Gaussian fit as the initial guess, then fit the 2D density with narrow and wide Gaussian components, mark the fitted center on heatmaps, and sample column/row profile lines through that fitted center without symmetrization.
  Brief: Replaced the previous symmetric profile-tail calculation in `script/anlz_ssntfr_2d.jl` with `fit_two_gaussian_1d_guess`, `fit_two_gaussian_2d`, and fitted-center cross-section helpers. The 1D guess fits the wide tail beyond `20 μm` first, estimates the narrow peak from the residual, and seeds a bounded 2D two-concentric-Gaussian fit per `(IB, istp)`. Heatmap crosses now mark fitted `(x0, y0)`, profile panels use interpolated column/row slices through that center with common full-range x-limits, and tailess curves subtract the wide 2D Gaussian plus background. Corrected the displayed row/column profile semantics so column is the vertical cross-section at fixed fitted `x0` and row is the horizontal cross-section at fixed fitted `y0`. Regenerated `SSNTFR_ntfr2d_table.png`, visually checked the fitted-center table, and ran `git diff --check` with only line-ending warnings.
- Request: Repeat the fitted-center SSNTFR 2D analysis with a Lorentzian center peak and Gaussian tail, while keeping the existing Gaussian model path available through an option on `fit_centered_density_profiles`.
  Brief: Added `model_center` selection to `fit_centered_density_profiles`, supporting `:gaussian` via the existing two-Gaussian model and `:lorentzian` via new Lorentzian-center 1D/2D model functions that keep the same parameter order `(x0, y0, A_narrow, width_narrow, A_wide, σ_wide, B)`. Set the active script to `model_center = :lorentzian`, retained Gaussian-tail subtraction and the same plotting layout, and adjusted the 2D optimization to use `fit_stride_2d = 3` with `fit_maxiter_2d = 10_000` for practical runtime. Fixed all background offsets to zero by fitting only `(A_wide, σ_wide)` in the 1D tail stage, fitting only the first six 2D parameters, and appending `B = 0.0` to stored fit params for compatibility. Regenerated `SSNTFR_ntfr2d_table.png`, visually checked the Lorentzian-center zero-background table, and ran `git diff --check` with only line-ending warnings.
- Request: Add two diagnostic columns to the SSNTFR 2D table, one per isotope, showing fitted-center cross sections in log scale without tail removal; fold positive/negative sides onto the same distance axis and overlay faint column and stronger row traces.
  Brief: Added `draw_folded_branch!` and appended two equal-width folded-log diagnostic columns to `script/anlz_ssntfr_2d.jl`. Each diagnostic panel plots four positive-valued original-profile branches on a log y-axis: column positive/negative branches in fainter isotope color and row positive/negative branches in stronger isotope color, with dashed lines for the negative side. Expanded the figure to eight columns, added diagnostic headers, computed common log y-limits from all positive original fitted-center profile samples, regenerated `SSNTFR_ntfr2d_table.png`, visually checked the new columns, and ran `git diff --check` with only line-ending warnings.
- Request: Use a Gaussian two-peak 2D fit only to determine fitted centers, then refit the density tail on the fixed-center outer rim `r > 20 μm` with a log-scale Gaussian disk; use this rim-fitted tail for tail removal and show it as a green line in folded-log diagnostics, without dashed branch styling.
  Brief: Set `model_center = :gaussian` for the center-finding step and added `fit_log_gaussian_rim_tail`, which fits `log(A) - r²/(2σ²)` against positive 2D rim-density samples measured from each fitted center. `fit_centered_density_profiles` now stores `fit_tail_rim` and builds column/row profiles using the rim-fitted Gaussian tail rather than the center model's wide component. The tailess profile panels use this new tail, folded-log diagnostics draw the fitted tail as a green line, and positive/negative folded branches are all solid with row/column distinguished by opacity/linewidth. Updated the final tail fit to use the fixed annulus `17-37 μm`, fixed folded-log y-limits to start at `1e-2`, and marked each folded-log panel with the fitted tail `σ` in green. Regenerated `SSNTFR_ntfr2d_table.png`, visually checked the log-rim tail overlays and labels, and ran `git diff --check` with only line-ending warnings.
- Request: Move the SSNTFR 2D workflow to output title `04.Ntfr2D`, return to a two-step fit by skipping the separate rim-tail fit, and make the second fit a log-scale concentric double-Gaussian disk with image values masked below `3e-1`, anisotropic no-rotation narrow Gaussian widths, wide Gaussian `σ > 15 μm`, narrow-peak shading in row/column profiles, heatmap ticks visible, and a copy of the script saved to the output folder.
  Brief: Updated `script/anlz_ssntfr_2d.jl` to write under `AnlzRoutine\04.Ntfr2D`. Added `double_gaussian_disk_2d_model` and `log_double_gaussian_disk_2d_model`; the active second-stage fit now fits logged model values to logged image values above `fit_threshold_log_2d = 3e-1`, with parameters `(x0, y0, A_narrow, σx_narrow, σy_narrow, A_wide, σ_wide)` and `fit_sigma_wide_min = 15.0`. Profile extraction now uses the wide component from this fit as the tail and the anisotropic narrow component for per-axis core shading. Row/column panels shade the narrow peak over the tail behind the lines, folded-log panels draw the `3e-1` threshold line, 2D heatmap tick labels remain visible, and the script copies itself into the output folder after saving. Ran the script on real `prfl.h5`, visually checked `04.Ntfr2D\SSNTFR_ntfr2d_table.png`, verified the copied script is present, and ran `git diff --check` with only line-ending warnings.
- Request: Advance the SSNTFR 2D workflow to output title `05.Ntfr2D`, keep the manually lowered log-fit threshold `1.5e-1`, and restore all bottom-row x ticks and labels.
  Brief: Updated `script/anlz_ssntfr_2d.jl` to write under `AnlzRoutine\05.Ntfr2D`, preserved `fit_threshold_log_2d = 1.5e-1`, and refined axis decoration handling. After an all-axis-label attempt made the table too tall and cropped, restored the 03-style layout with first-row column labels, x ticks/tick labels only on the last row, y tick labels visible, and `resize_to_layout!(fig_ntfr)` before save instead of a hand-set figure size. Ran the script on real `prfl.h5`, visually checked `05.Ntfr2D\SSNTFR_ntfr2d_table.png`, verified the copied script is present in the output folder, and ran `git diff --check` with only line-ending warnings.
- Request: Use the newly added aberrated double-Gaussian 2D model for the active logged fit, adding a bounded `β` parameter below `0.2`, and write a fresh output under a new title with the same settings.
  Brief: Advanced `script/anlz_ssntfr_2d.jl` to `title_anlz = "06.Ntfr2D.Abrr"`. Fixed the new `double_gaussian_disk_2d_model_abrr` helper so it calls `double_gaussian_disk_2d_model(coords, params_ideal)` in the correct argument order, added `log_double_gaussian_disk_2d_model_abrr`, and switched the active second-stage fit to that logged aberration model. The fit now appends `β` to the existing seven-parameter vector with initial value `0.01` and bounds `0 <= β <= 0.2`. Row/column profile panels now shade only the center Gaussian from zero: a lighter approximate aberration-boosted center shade `N + βN²` behind the darker raw center Gaussian `N`, with `β` labeled in the top-left of every row/column axis; the tail line remains `T + βT²`. Ran the script on real `prfl.h5`, visually checked `06.Ntfr2D.Abrr\SSNTFR_ntfr2d_table.png`, verified the copied script is present in the output folder, and ran `git diff --check` with only line-ending warnings.
- Request: Try the aberrated double-Gaussian 2D fit again under a new title, but fit in linear image scale instead of logging the model and data first.
  Brief: Advanced `script/anlz_ssntfr_2d.jl` to `title_anlz = "07.Ntfr2D.Abrr.LinearWeight"` and changed the active second-stage fit from `log_double_gaussian_disk_2d_model_abrr` against `log.(z)` to `double_gaussian_disk_2d_model_abrr` directly against masked image values. Kept the same threshold mask, β bounds, wide-σ lower bound, component shading, and layout. Ran the script on real `prfl.h5`, visually checked `07.Ntfr2D.Abrr.LinearWeight\SSNTFR_ntfr2d_table.png`, verified the copied script is present in the output folder, and ran `git diff --check` with only line-ending warnings.
- Request: Change the aberrated SSNTFR 2D fit so the β increase acts only on the center peak, allow β up to `2.0`, and draw the boosted center shade in front of the profile lines with transparency.
  Brief: Updated `double_gaussian_disk_2d_model_abrr` in `script/anlz_ssntfr_2d.jl` to model `tail + center + β center²`, leaving the wide Gaussian tail unboosted. Profile extraction now keeps the tail as the raw wide component and uses `center + β center²` only for the boosted center shade. Row/column profile panels draw the raw center shade behind the lines and the transparent boosted center shade after the lines, with β labels retained. Confirmed the active β upper bound is `2.0`, updated the figure title text to `βN²`, reran the script on real `prfl.h5`, visually checked `09.Ntfr2D.Abrr.LinearWeight\SSNTFR_ntfr2d_table.png`, verified the copied script is present, and ran `git diff --check` with only line-ending warnings.
- Request: Reconstruct every `(IB, istp)` density image from the latest aberrated 2D fit, convert each reconstructed density to a 1D FT modulation profile with `calc_prfl_modl_1d` using `smwh=(150, 150)` and the measured `x_modl` step, and add per-istp comparison columns to `fig_ntfr` against `prfl_inco` and `prfl_cohr`.
  Brief: Advanced `script/anlz_ssntfr_2d.jl` to `title_anlz = "10.Ntfr2D.Abrr.LinearWeight.ComparePrflModl"`. Added explicit `FFTW` and `ImageFiltering` imports, `orient_prfl_axes`, reconstruction helpers for `double_gaussian_disk_2d_model_abrr`, and reconstructed-profile generation using the measured `step_modl = 0.01131282051282051`. Added two `FT prfl` columns, plotting reconstructed profiles in fit green, `prfl_inco` in dashed theme color, and `prfl_cohr` in solid theme color with `x = 0..0.6` and `y = -0.1..0.6`. Fixed constructed-function bugs in `calc_prfl_modl_1d`: range-plus-scalar indexing now broadcasts, the profile mean is forced to a vector before windowing, and FFT magnitude uses `abs.`. Simplified saving to one `filename_plot_ntfr` loop over `png` and `pdf`. Ran the script on real `prfl.h5`, visually checked `10.Ntfr2D.Abrr.LinearWeight.ComparePrflModl\SSNTFR_ntfr2d_table.png`, verified the copied script and PDF are present, and ran `git diff --check` with only line-ending warnings.
- Request: Continue the `script/anlz_ssntfr_2d.jl` refactor so `ntfr2d_mean` follows the `_fmt` convention as a 2D `(IB, istp)` array whose elements are 2D density images, and downstream density reconstruction/profile calculation maps over those image payloads instead of using pure wrapper functions around 4D arrays.
  Brief: Changed `orient_ntfr2d_axes` to convert either accepted raw HDF5 layout into `Array{Matrix{Float64}}(n_IB, n_istp)`. Updated centered fitting, heatmap drawing, color/profile limit calculations, and reconstructed FT profile plotting to index `ntfr2d_mean[idx_IB, idx_istp]` image payloads. Removed `reconstruct_density_2d` and `calc_reconstructed_prfl_modl`; reconstruction now builds shared model coordinates once, then uses `map` over `fit_density` to produce `ntfr2d_fit::Array{Matrix}` and maps `calc_prfl_modl_1d` over those images to produce `prfl_modl_fit::Array{Vector}`. Verified syntax with `Meta.parseall`, verified `map` preserves formatted outer dimensions and `maximum(maximum, ntfr2d_fmt)` on a toy Julia array, and ran `git diff --check` with only the existing CRLF warning. A real HDF5 read check was blocked because this Julia 1.12.6 install cannot precompile `HDF5` until project dependencies/stdlib visibility are repaired (`Pkg` itself is not visible in that install's load path).
- Request: In `script/anlz_ssntfr.jl`, change only the cohr tail-removal path to subtract the reconstructed `prfl_modl_fit` profiles from the 2D NTFR analysis for matching `(IB, istp)`, keep inco processing/presentation unchanged, and avoid copying all 2D helper functions by moving shared code into one source file.
  Brief: Added `src/modlntfr.jl` with shared profile/NTFR orientation, 2D aberrated double-Gaussian fitting, fitted-center profile extraction, density reconstruction, FT modulation-profile generation, and packing helpers. Updated `script/anlz_ssntfr_2d.jl` to include the shared file and removed its local copies of those helpers. Updated `script/anlz_ssntfr.jl` to read `x_dens`/`ntfr2d_mean`, run `fit_reconstructed_prfl_modl`, pack the resulting `(IB, istp)` model profiles into `(x_modl, istp, IB)`, and form `tail_cohr.prfl_cohr_tailess` by subtracting those profiles from `prfl_cohr`; inco tail fitting and diagnostic rendering remain unchanged. Cohr average visualization now draws the reconstructed 2D NTFR model profile in green instead of the previous common fitted tail plus sidepeak. Verified all touched Julia files with `Meta.parseall`, ran `git diff --check` with only CRLF warnings, then ran `script/anlz_ssntfr.jl` on real `prfl.h5`; it saved all four expected PNGs under `AnlzRoutine\13.IncoNvlpCohrModlNtfrTail`.
- Request: Use the same reconstructed 2D NTFR model-profile tail subtraction for inco as well as cohr in `script/anlz_ssntfr.jl`, and test by running the script.
  Brief: Updated `script/anlz_ssntfr.jl` to write under `AnlzRoutine\14.IncoCohrModlNtfrTail` and use `calc_modl_tail(prfl, prfl_modl_fit)` for both `prfl_inco` and `prfl_cohr`. Removed the old inactive inco curve-fit tail code and simplified the tail diagnostic duet so both inco and cohr panels show original versus model-tail-subtracted profiles. Verified with `Meta.parseall`, then ran `script/anlz_ssntfr.jl` on real `prfl.h5`; it saved all four expected PNGs under `AnlzRoutine\14.IncoCohrModlNtfrTail`.
- Request: In `script/anlz_ssntfr_2d.jl`, add two 2D residual-profile columns after the original density heatmaps, one per isotope, showing original density minus reconstructed density with the PCA turquoise/violet positive/negative colormap and one shared residual color limit across all panels; test by running.
  Brief: Moved `gen_clrmap_posneg_nonlin` from `src/vispca.jl` into shared `src/graphics.jl`, preserving the PCA mode turquoise/violet colormap formula. Updated `script/anlz_ssntfr_2d.jl` to compute `ntfr2d_diff = ntfr2d_mean - ntfr2d_fit`, derive a global symmetric `colorrange_diff`, and insert two residual heatmap columns immediately after the density columns. Shifted all subsequent profile/diagnostic/FT columns and headers to the new 12-column layout, updated the title with the residual limit, and kept fitted-center crosshairs on residual panels. Verified parse checks for touched files and ran `script/anlz_ssntfr_2d.jl` on real `prfl.h5`; it saved `SSNTFR_ntfr2d_table.png` under `AnlzRoutine\15.Ntfr2D.Abrr.LinearWeight.WithDiff`.
- Request: Edit `script/anlz_ssntfr_2d.jl` so the residual heatmaps are normalized by each shot/image, and do not run the script.
  Brief: Added `calc_normalized_density_diff`, which validates matching image sizes and positive normalizations, then computes `dens2d / sum(dens2d) - dens_fit2d / sum(dens_fit2d)` per `(IB, istp)` image. Updated `ntfr2d_diff` to use this shot-normalized residual, changed the figure title to report normalized residual limits, and relabeled residual columns as `norm diff`. Per request, did not run the analysis script.
- Request: Roll the `script/anlz_ssntfr_2d.jl` residual columns back to unnormalized density differences, use independent residual color scales, and fix the large gap between the two residual isotope columns.
  Brief: Reverted `ntfr2d_diff` to raw `ntfr2d_mean - ntfr2d_fit` and removed the shot-normalized residual helper. Changed residual color limits from one global symmetric range to per-panel symmetric ranges stored as `colorrange_diff[idx_IB, idx_istp]`, while keeping the title's `max residual` as a global reference. Restored residual column labels to `diff`, and changed residual columns 3 and 4 from `Fixed(170)` to `Fixed(105)` so their square heatmaps sit together like the original density pair. Verified with `Meta.parseall`; did not run the full analysis script.
- Request: Update `src/modlntfr.jl` and the `script/anlz_ssntfr_2d.jl` workflow for the new 2D peak-asymmetry parameters `skew_x` and `skew_y`, with neutral initial skew and bounds from negative to positive values, and point out model bugs found while testing.
  Brief: Added explicit `SpecialFunctions.erf` import and completed the skewed aberrated 2D model integration with 10 parameters `(x0, y0, A_narrow, σx_narrow, σy_narrow, A_wide, σ_wide, β, skew_x, skew_y)`. The 2D fit now initializes `skew_x = skew_y = 0.0` and bounds both in `[-10, 10]`; `double_gaussian_disk_2d_model_abrr` validates the 10-parameter model. Fixed two model bugs where `@.` incorrectly broadcast `gauss_skew_com_unit` over the `(σ, skew)` tuple, and fixed vector/scalar broadcasting inside the skew helper. Updated centered cross-section decomposition so shaded narrow profiles use the skewed model slice, including the fixed orthogonal skew factor. Updated the 2D table title to describe the skew Gaussian peak. Verified touched files parse and `git diff --check` passes with only CRLF warnings. Full `script/anlz_ssntfr_2d.jl` execution was interrupted after a long fit runtime; a real-data one-panel smoke test completed with `n_params=10`, `β/skew=[0.007189763192041742, 0.3079293068816521, 8.847893184372947e-5]`, `rss_rel=0.12787284243805014`, reconstructed image size `(301, 301)`, and profile length `241`.
- Request: Add console progress logging to the long-running `script/anlz_ssntfr_2d.jl` workflow, especially per-IB fitting, axis/figure building, plotting, and saving, and show fitted `α_x`/`α_y` beside `β` in column/row tailess panels; then run the full script.
  Brief: Added `log_step`/`log_done` helpers in the SSNTFR 2D script following the excitation script style, with stage timings for range preparation, centered 2D fitting, reconstruction, plot-range building, figure axis/label building, density-row plotting, and output saving. Extended `fit_centered_density_profiles` with optional `log_tag`, `val_IB`, and `val_istp` metadata so every `(IB, istp)` 2D fit prints before and after, including `rss`, `β`, `α_x`, and `α_y`. Tailess profile annotations now show `β`, `α_x`, and `α_y`. Verified parse checks and `git diff --check`; ran `script/anlz_ssntfr_2d.jl` on real `prfl.h5` to completion. The centered 2D fit stage took `970.4 s`, plotting density rows took `50.7 s`, saving took `8.7 s`, and output saved to `...\DualSS\AnlzRoutine\16.Ntfr2D.Abrr.Skewed\SSNTFR_ntfr2d_table.png`.
- Request: Complete the new `θ` rotation parameter in the SSNTFR 2D aberrated model while keeping `skew_x` and `skew_y` locked at zero, update the workflow/diagnostics, and test with `script/anlz_ssntfr_2d.jl`.
  Brief: Extended `double_gaussian_disk_2d_model_abrr` to 11 parameters, rotating the narrow peak coordinates around `(x0, y0)` by `θ` before evaluating the narrow axis factors, while leaving the wide Gaussian tail unrotated. Updated `fit_two_gaussian_2d` with `θ` initial value `0.0` and bounds `[-π/2, π/2]`, preserving locked skew bounds at `0.0`. Updated centered row/column profile decomposition to evaluate the exact rotated narrow cross-section, added `θ` to fit logging and tailess annotations, changed the active output title to `18.Ntfr2D.Abrr.Rotated.NoSkew`, and fixed the provenance copy of `modlntfr.jl` to write into `path_output`. Removed the stray root-level `modlntfr.jl` generated by the previous bad copy target. Verified syntax with `Meta.parseall`, ran `git diff --check`, and ran `script/anlz_ssntfr_2d.jl` on real `prfl.h5` to completion; fit stage took `67.7 s`, plotting rows took `73.3 s`, saving took `7.1 s`, and output saved to `...\DualSS\AnlzRoutine\18.Ntfr2D.Abrr.Rotated.NoSkew\SSNTFR_ntfr2d_table.png`.
- Request: Add a visual outline of the rotated narrow elliptical peak on the first two density columns, and show the tilted local `y` orientation axis while keeping the model convention `θ = 0` as local `x` along lab `x`.
  Brief: Added `calc_rotated_ellipse` and `calc_rotated_y_axis` to `script/anlz_ssntfr_2d.jl`. The first two density heatmap columns now overlay a `1σ` rotated narrow ellipse plus a heavier line along the fitted local `y` axis; the model and profile decomposition keep the standard convention `x_rot = cosθ dx + sinθ dy`, so `θ = 0` means local `x` is lab `x`. Verified parse and `git diff --check`, then reran `script/anlz_ssntfr_2d.jl`; fit stage took `97.5 s`, plotting rows took `56.5 s`, saving took `9.0 s`, and the updated table was saved to `...\DualSS\AnlzRoutine\18.Ntfr2D.Abrr.Rotated.NoSkew\SSNTFR_ntfr2d_table.png`.
- Request: Enforce the narrow peak width convention `σ_x < σ_y`, make the density-column ellipse marker thin white with the center-crosshair linewidth, and draw the rotation indicator only outside the ellipse along the long axis; regenerate the same output folder.
  Brief: Reparameterized the 2D fit internally with `delta_sigma_y >= 0`, evaluating and storing actual model parameters with `σ_y = σ_x + delta_sigma_y` so the local `x` width is never larger than the local `y` width. Updated the original-density overlays to use a thin white `1σ` ellipse with linewidth `0.7`, and replaced the internal orientation diameter with two outside-only white line segments along the long/local-`y` axis. Verified parse checks and `git diff --check`, then reran `script/anlz_ssntfr_2d.jl` into `18.Ntfr2D.Abrr.Rotated.NoSkew`; fit stage took `74.1 s`, plotting rows took `73.3 s`, saving took `13.6 s`, and `SSNTFR_ntfr2d_table.png` was regenerated.
- Request: Reflect the new skewed-background `double_gaussian_disk_2d_model_abrr` in SSNTFR 2D tailess column/row profiles, add axis-specific `sigma_narrow` and `alpha` labels, keep `theta` labels, and run `script/anlz_ssntfr_2d.jl`.
  Brief: Updated `double_gaussian_disk_2d_model_abrr` so the wide tail evaluates precomputed skew factors instead of broadcasting over `(σ, α)` tuples, matching the narrow-component pattern. Updated `calc_centered_cross_profile` so `tail_raw` and the gray profile tail use the same rotated/skewed wide component as the fitted model, so tailess subtraction follows the skew angle. Tailess profile annotations now show `β`, `σ_y`/`α_y`, and `θ` on column axes, and `β`, `σ_x`/`α_x`, and `θ` on row axes. Advanced the output folder to `20.Ntfr2D.Abrr.Rotated.SkewedYWithTail`. Repaired the local Julia project with `Pkg.instantiate()` after `SpecialFunctions` failed to load, verified parse checks and `git diff --check` with only CRLF warnings, then ran `script/anlz_ssntfr_2d.jl` to completion; fit stage took `1112.6 s`, plotting rows took `51.7 s`, saving took `7.6 s`, and output saved to `...\DualSS\AnlzRoutine\20.Ntfr2D.Abrr.Rotated.SkewedYWithTail\SSNTFR_ntfr2d_table.png`.
- Request: Update SSNTFR 2D visualization so FT profile panels show `rss_rel` in the top-right using green text normally and brown when the fit reaches max iterations, add a mirrored final IB column, and place script text annotations with relative axis coordinates near the same corners.
  Brief: Added `maxiter_reached = !fit.converged` to `fit_two_gaussian_2d` results and included `maxiter=true` in fit progress logs when applicable. FT profile panels now draw `rss_rel` at relative `(0.95, 0.95)` in fit green or brown for max-iteration fits. Existing density/profile/folded-log text annotations now use `space=:relative` with 0.05 edge offsets while preserving their corner placement. Added a final right-side IB column and header mirroring the leading IB labels, expanded the title span and layout sizing/gaps to 13 columns. Verified parse checks and `git diff --check` with only CRLF warnings, then ran `script/anlz_ssntfr_2d.jl` to completion; fit stage took `1142.7 s`, plotting rows took `54.8 s`, saving took `9.0 s`, and output saved to `...\DualSS\AnlzRoutine\20.Ntfr2D.Abrr.Rotated.SkewedYWithTail\SSNTFR_ntfr2d_table.png`.
- Request: Modify the SSNTFR 2D aberrated model to fit separate y-skew parameters for the narrow Gaussian peak and wide tail, and fix the rotation angle `θ` to `0.30` by setting equal lower/upper bounds.
  Brief: Expanded `double_gaussian_disk_2d_model_abrr` and `expand_fit_params_abrr` from 11 to 12 parameters, storing `(skew_x, skew_y_narrow, skew_y_tail, θ)` after `β`. The narrow component now evaluates with `α_y,narrow`, while the wide tail and centered-profile `tail_raw` use `α_y,tail`; `α_x` remains shared and locked at zero. Updated fit initialization and bounds so `θ` starts at `0.30` and has lower/upper bounds both set to `0.30`, with separate `[-10, 10]` bounds for the two y-skews. Updated fit logs and column-profile annotations to show `α_y,N` and `α_y,T`, moved `θ` indexing to parameter 12, and advanced the output folder to `21.Ntfr2D.Abrr.Rotated.SeprSkewedYWithTail`. Verified parse checks, confirmed LsqFit accepts equal bounds on a fixed parameter, and ran `script/anlz_ssntfr_2d.jl` to completion; fit stage took `1558.3 s`, plotting rows took `51.1 s`, saving took `8.4 s`, and output saved to `...\DualSS\AnlzRoutine\21.Ntfr2D.Abrr.Rotated.SeprSkewedYWithTail\SSNTFR_ntfr2d_table.png`.
- Request: Halt the high-iteration SSNTFR 2D test, save fitted model and modulation-profile outputs to a Julia-native file as well as HDF5, and run a low-iteration smoke test first.
  Brief: Stopped the active high-iteration Julia session after the process exited while polling. Preserved `title_anlz = "22.Ntfr2D.Abrr.Rotated.SeprSkewedYWithTail.LargeIter"` and default `fit_maxiter_2d = 100000`, adding an `SSNTFR_2D_MAXITER` environment override for smoke tests. Added packed result helpers and end-of-script model output saving to both `SSNTFR_ntfr2d_model_results.h5` and `SSNTFR_ntfr2d_model_results.jld2`; both contain axes, `fit_params`, `fit_rss_rel`, `fit_maxiter_reached`, `ntfr2d_fit`, `ntfr2d_diff`, `prfl_modl_fit`, and metadata/dimension labels. Verified parse checks and `git diff --check`, ran a smoke test with `SSNTFR_2D_MAXITER=1`, and confirmed the JLD2 output loads with `fit_params (12,2,17)`, `ntfr2d_fit (301,301,2,17)`, and `prfl_modl_fit (241,2,17)`. Per request, did not restart the long high-iteration run after the smoke test.
- Request: In the current SSNTFR 2D trial, remove `skew_x` altogether and lock `skew_y_narrow` to zero while leaving the y-skewed tail parameter available.
  Brief: Reduced the aberrated 2D model and expanded fit parameter vectors from 12 to 11 parameters by removing `skew_x`; x-axis skew is now hard-coded neutral in narrow and tail components. Kept `skew_y_narrow` in the parameter/result metadata but fixed its fit bounds at `0.0`, while `skew_y_tail` remains bounded in `[-10, 10]`. Updated centered cross-profile decomposition, fit logging, saved `fit_param_names`, density overlay `theta` indexing, and tailess annotations to match the shifted parameter order. Verified `src/modlntfr.jl` and `script/anlz_ssntfr_2d.jl` parse and ran `git diff --check`; per user request, did not run the full analysis script.
- Request: Redo SSNTFR 2D test 15 under `24.Ntfr2D.Abrr.LinearWeight.WithDiff.[redo.15]`, restoring the test-15 data-processing model while keeping the latest visualization improvements and model-output saving.
  Brief: Compared the saved test-15 script in the OneDrive output folder against the current workflow. Rewound `src/modlntfr.jl` to the test-15 linear 8-parameter aberrated model: cocentered axis-aligned elliptical narrow Gaussian plus round Gaussian tail plus `β * narrow^2`, with direct linear-scale fitting, independent `σ_x`/`σ_y`, and `β` bounds `0..2`. Removed the skew/rotation fit surface, restored the default max iteration count to `10000`, and updated profile decomposition/logging to match the old model. Kept the latest script features: progress logging, per-panel residual heatmaps, axis-aligned fitted ellipse overlay, relative text placement, `rss_rel` coloring, mirrored right-side IB labels, provenance copies, and HDF5/JLD2 model-output saving. Verified both touched Julia files parse and `git diff --check` passes with only CRLF warnings; did not run the full analysis.
- Request: Run `script/anlz_ssntfr.jl` as analysis 25 using reconstructed model data read from 24's JLD2 output instead of recalculating reconstruction during the run.
  Brief: Updated `script/anlz_ssntfr.jl` to `title_anlz = "25.IncoCohrModlNtfr.[reconstr.24]"`, imported `JLD2`, and added `load_prfl_modl_fit_jld2` to read `SSNTFR_ntfr2d_model_results.jld2` from `24.Ntfr2D.Abrr.LinearWeight.WithDiff.[redo.15]`. The loader validates `x_modl`, `val_IB`, `val_istp`, and `prfl_modl_fit` dimensions before using the packed `(x_modl, istp, IB)` profile model directly. Removed the in-run `fit_reconstructed_prfl_modl`/`pack_prfl_modl_fit` calculation path. Verified parsing and ran the script successfully; output folder `...\DualSS\AnlzRoutine\25.IncoCohrModlNtfr.[reconstr.24]` contains fresh stacked, line, tail diagnostic, and coherent-average PNGs.
- Request: Recreate the Mathematica `reference.wl` source-profile generation from `0204_interference/result/data.h5` in Julia, using source 2D density stacks to generate incoherent/coherent modulation profiles for the existing `anlz_ssntfr.jl` plotting workflow under title `28.IncoCohrModlNtfr.[WL-migration]`.
  Brief: Added `script/anlz_ssntfr_src.jl` to load raw source images into an `(IB, istp, rep)` image-element array, with the source-file inversion handled explicitly so `istp=162` maps to `im64us` and `istp=164` maps to `im62us`. The script now follows the `prfl.h5`/reference FFT input path by using all 100 raw shots per `(IB, istp)`, no valid-duet filtering, no corner-background subtraction, and fixed-center crops at `(201, 201)` to `301 x 301` before mapping the image vectors through `calc_prfl_modl_1d`. Updated `src/modlntfr.jl` so `calc_prfl_modl_1d` supports both a single reconstructed density image and a vector of source images, with coherent/incoherent averaging only on the vector path, factored the modulation profile constants into `get_prfl_modl_1d_config`, and switched the modulation direction to horizontal `x`: the density strip is narrow in `y` via `smh_dens_strip`, averaged over `y`, then FFTed along `x` with `smw_modl`. Added `use_src_profiles` to `script/anlz_ssntfr.jl`; when true it includes the source script, saves `SSNTFR_prfl_src.h5`, and writes plots to `...\DualSS\AnlzRoutine\28.IncoCohrModlNtfr.[WL-migration]`. Added a `17 x 2` source mean-density table with row-0 IB labels, isotope theme colormaps, one common color range, transposed heatmap payloads so `x` is horizontal and `y` vertical, and a thin horizontal strip rectangle showing the exact region used for modulation-profile extraction; regenerated `SSNTFR_ntfr2d_mean_src_table.png`. Verified `x_dens` agrees with `prfl.h5` to roundoff and reported that recomputed `x_modl` is the symmetric `-120Δk:120Δk` axis while `prfl.h5` is shifted by one bin. Later removed the tiny `pack_src_prfl` helper and inlined its profile-cube packing at the script call site. Verified touched files parse and reran `script/anlz_ssntfr_src.jl`; the script printed successful save of the fixed-center mean-density table. `git diff --check` passes with only CRLF warnings.
- Request: Remove hard-coded references to `24` from `script/anlz_ssntfr.jl` strings and symbol names.
  Brief: Renamed `path_model_results_24` to neutral `path_model_results` and changed `load_prfl_modl_fit_jld2` validation messages from `24 reconstructed ...` to `Reconstructed model ...`. Confirmed `script/anlz_ssntfr.jl` has no remaining literal `24` references and parsed successfully with Julia 1.12.6.
- Request: Bring source-profile shot selection and peak-centered cropping back into `script/anlz_ssntfr_src.jl`, using a 2D `(IB, istp)` array whose entries are vectors of selected `301 x 301` crops.
  Brief: Replaced the fixed-center rectangular crop stack with `dens_src_core`, an `(IB, istp)` array of variable-length vectors selected by duet number and center validity. Inlined the one-time workflow steps in the script body: source-shot number medians, duet masks, WL-style Gaussian-plus-offset center fits, peak-centered crops, vector-based mean images, and direct vector input to `calc_prfl_modl_1d` without `eachslice`. Renamed the center filter parameter from radius to `sigma_center_filter_src` to reflect Julia `Kernel.gaussian` semantics, and updated `AGENTS.md` to prefer inline script workflow steps for short one-off processing such as selection masks and centering/cropping passes. Parsed successfully, reran `script/anlz_ssntfr_src.jl`, regenerated `SSNTFR_ntfr2d_mean_src_table.png`, and matched the recorded Mathematica accepted counts exactly: `[82, 78, 75, 72, 80, 75, 83, 79, 80, 66, 79, 69, 77, 75, 72, 75, 76]`.
- Request: Apply the verified `calc_prfl_modl_1d([ds], ...)` library path and x-horizontal image orientation convention to `script/anlz_ssntfr_2d.jl`, without adding new library functions, then run the active 2D analysis.
  Brief: Checked the simplified `modlntfr.jl` profile API and kept reconstructed modulation profiles routed through `calc_reconstructed_prfl_modl`, which calls `calc_prfl_modl_1d([dens2d], smwh; step_modl).prfl_inco`. Fixed existing 2D fitting/reconstruction helpers to use matrix storage as `(y, x)`: fit/reconstruction coordinates now pair `vec(dens2d)` with x repeated by column and y varying down rows, and bilinear interpolation indexes `dens2d[y, x]`. Updated `script/anlz_ssntfr_2d.jl` heatmaps to plot transposed matrices so x is horizontal, and changed saved model metadata dimensions to `y_dens,x_dens,istp,IB`. Verified parsing and `git diff --check`, then ran the full active analysis `29.Ntfr2D.Abrr.LinearWeight.Lib`; it completed successfully and wrote `SSNTFR_ntfr2d_table.{png,pdf}` plus H5/JLD2 model outputs.
- Request: Rewrite `interp2_bilinear` using `NaturalNeighbours.jl` instead of manual index arithmetic.
  Brief: Added `NaturalNeighbours` import in `src/modlntfr.jl` and rewrote `interp2_bilinear` to build a natural-neighbor interpolant over image-grid sites `(x, y)` paired with `vec(dens2d)`, preserving explicit out-of-range `NaN` behavior. Added a vector-query method so `calc_centered_cross_profile` builds the interpolant once per profile instead of once per point, with the scalar method as a wrapper. Verified parsing, `git diff --check`, and a tiny runtime smoke test: center query returned `55.0`, while an out-of-range query returned `NaN`.
- Request: Debug the skew-x SSNTFR 2D model after lowering `fit_maxiter_2d` exposed a runtime issue.
  Brief: Reproduced the failure in `script/anlz_ssntfr_2d.jl`: cross-profile reconstruction crashed after the first fit because `@.` broadcasted into the `(sigma, skew)` parameter tuple in `gauss_skew_com_unit(...)`, producing a `301` vs `2` dimension mismatch. Fixed `calc_centered_cross_profile` by computing skew component vectors before broadcasting products. Also fixed the NaturalNeighbours performance bottleneck by using local two-grid-line natural-neighbor interpolants for row/column profile queries instead of triangulating the full `301 x 301` image. Verified parsing, `git diff --check`, a tiny interpolation smoke test, and a full low-iteration run with `SSNTFR_2D_MAXITER=1`; outputs were written to `31.Ntfr2D.Abrr.LinearWeight.SkewX`.
- Request: In `script/test_ssntfr_2d_profile.jl`, add a live GLMakie inspection window for a selected SSNTFR 2D mean-density image after partially running `script/anlz_ssntfr_2d.jl`, with top-level `ib`, `istp`, row, and column selections in micrometers; then put the row profile below the heatmap, the column profile to the right, mark the 41-pixel mean strips, let heatmap clicks move the selected row/column, disable drag rectangle zoom, and add four buttons for cycling `ib`/`istp`.
  Brief: Added configurable selections plus helpers for closest-coordinate lookup, center 41-pixel averaging, coordinate span edges, and rectangle-zoom disabling. Refactored the inspector to take the full `ntfr2d_mean` matrix and `val_istp`, with observables for active `IB`, `istp`, density image, colormap, color range, selected row/column, selected profiles, and center-strip mean profiles. The script now draws a 2D density heatmap with the existing isotope colormap, very light gray `hspan!`/`vspan!` center-strip shading placed behind the heatmap, and observable theme-colored selected row/column markers; the selected row profile sits below the heatmap, the sideways selected column profile sits to the right, and gray background profiles show means over the centered 41 rows/columns with density range `(-1, 15)`. Added an `events(fig).mousebutton` callback so left-clicking inside the heatmap snaps to the nearest `x_dens` coordinate and updates the cuts, and subdivided `fig[2,2]` into four arrow `Button`s where up/down cycle `IB` and left/right cycle `istp`; observer handles are kept in the returned `profile_axes` tuple. Verified `script/test_ssntfr_2d_profile.jl` parses and `git diff --check` passes; did not execute the live GL window because it depends on in-memory variables from a partial analysis run, and a separate GLMakie package-load smoke check was blocked by the local Julia `longpath: Access is denied` issue.
- Request: Create `script/anlz_ssntfr_1d.jl` analogous to the active 2D NTFR workflow, but fit the center-strip 1D row profile using independent skew and gamma parameters for narrow and wide Gaussian components, reconstruct the FT profile from a 1-pixel-high fitted density strip with `smwh=(150, 0)`, simplify the table to row-only profile/folded-log/FT columns, and test under title `32.Ntfr1D.Abrr.Gamma.Skew.Independent`.
  Brief: Added 1D helpers in `src/modlntfr.jl`: `skewed_gauss_1d`, `double_skew_gamma_gauss_1d_model`, center-row-strip profile extraction, bounded 9-parameter fitting, and a `fit_centered_density_profiles_1d` wrapper with progress logs. Adjusted `get_prfl_modl_1d_config` so zero-height reconstruction strips use `smh_dens_strip=0`, enabling `calc_prfl_modl_1d([reshape(fit.fit, 1, :)], (150, 0); step_modl)`. Added `script/anlz_ssntfr_1d.jl` with row-tailess, folded-log row-only, and FT-profile comparison columns, plus H5/JLD2 outputs storing fit parameters, fitted profile components, RSS/maxiter flags, and reconstructed modulation profiles. Verified both touched Julia files parse, ran a low-iteration smoke test, then ran the full default analysis; outputs were saved under `...\DualSS\AnlzRoutine\32.Ntfr1D.Abrr.Gamma.Skew.Independent\SSNTFR_ntfr1d_table.{png,pdf}` plus `SSNTFR_ntfr1d_model_results.{h5,jld2}`. `git diff --check` passes with only the existing line-ending warning.
- Request: Update the SSNTFR 1D model after local edits so the former gamma parameters are beta parameters integrated inside `skewed_gauss_1d`, with beta bounds `[-2, 2]` for both narrow and wide components, and display the fitted skews/betas in the tailess columns while testing in the same output folder.
  Brief: Renamed the active 1D model path to `double_skew_beta_gauss_1d_model` and `fit_skew_beta_gauss_1d`, fixed the parameter unpacking to `(x0, A_narrow, σ_narrow, skew_narrow, beta_narrow, A_wide, σ_wide, skew_wide, beta_wide)`, and removed the old extra gamma exponent application so the model is now simply the sum of the two beta-integrated skewed Gaussian components. Set both beta fit bounds to `[-2, 2]`, updated progress logs, H5/JLD2 metadata names, title text, and row-tailess annotations to show `αN/βN` and `αT/βT`. Left `title_anlz` unchanged so outputs overwrite the same `32.Ntfr1D.Abrr.Gamma.Skew.Independent` folder. Verified parse checks, ran a low-iteration smoke test, then ran the full default script successfully; refreshed `SSNTFR_ntfr1d_table.{png,pdf}` and `SSNTFR_ntfr1d_model_results.{h5,jld2}` in the same folder. `git diff --check` passes with only line-ending warnings.
- Request: Restore original 2D density columns to the SSNTFR 1D figure table, add a transparent beta-zero tailess fit reference in the row-tailess panels, remove the extra `fit_centered_density_profiles_1d` wrapper by moving its loop into the script, and keep beta lower bounds at `-1.0`.
  Brief: Removed `fit_centered_density_profiles_1d` from `src/modlntfr.jl`; `script/anlz_ssntfr_1d.jl` now owns the `(IB, istp)` loop around `calc_center_row_strip_profile` and `fit_skew_beta_gauss_1d` while preserving per-panel progress logs. Kept beta bounds as `[-1, 2]` for both components. Extended `fit_skew_beta_gauss_1d` results with `narrow_beta0`, `tail_beta0`, and `tailess_beta0`; the script now saves `fit_tailess_beta0` in H5/JLD2. The figure table now starts with two original-data 2D density heatmap columns, with no fitted overlays, followed by row tailess, folded-log, and FT-profile columns. Tailess panels draw a slightly darker transparent theme-color beta-zero narrow shade and faint beta-zero tailess line behind the beta-active tailess fit. Verified parse checks, ran a low-iteration smoke test, then ran the full default script; refreshed `SSNTFR_ntfr1d_table.{png,pdf}` and `SSNTFR_ntfr1d_model_results.{h5,jld2}` under the same `32.Ntfr1D.Abrr.Gamma.Skew.Independent` folder. `git diff --check` passes with only line-ending warnings.
- Request: Try the current SSNTFR 1D beta/skew settings with a log-space fit objective, keep the existing output displays, change the folded-log annotation from `x0` to `σ_wide`, and test under `33.Ntfr1D.Abrr.Beta.Skew.Independent.Log`.
  Brief: Added `log_double_skew_beta_gauss_1d_model` and a `fit_log` keyword to `fit_skew_beta_gauss_1d`, preserving the current initial guesses and bounds while fitting `log(model)` to `log(profile)` above the existing threshold. Updated `script/anlz_ssntfr_1d.jl` to `title_anlz = "33.Ntfr1D.Abrr.Beta.Skew.Independent.Log"`, set `fit_log_1d = true`, saved that flag in H5 metadata/JLD2 meta, included `log=true` in progress logs, and changed folded-log panel text to show fitted `σT`/`σ_wide` instead of `x0`. Verified parse checks, ran a low-iteration smoke test, then ran the full default analysis; outputs were saved under `...\DualSS\AnlzRoutine\33.Ntfr1D.Abrr.Beta.Skew.Independent.Log\SSNTFR_ntfr1d_table.{png,pdf}` plus `SSNTFR_ntfr1d_model_results.{h5,jld2}`. `git diff --check` passes with only line-ending warnings.
- Request: Migrate the interactive preview behavior from `script/test_ssntfr_2d_profile.jl` into `script/test_ssntfr_src_read.jl`, replacing its primitive static source-density preview and arranging the three source profile selection indices in a row in `fig[2,2]`.
  Brief: Rebuilt `script/test_ssntfr_src_read.jl` as a GLMakie inspector for `dens_src_core` after partially running `script/anlz_ssntfr_src.jl`. The preview now shows a selected source crop heatmap with center-strip shading, click-to-move row/column cut markers, a row profile panel below, and a sideways column profile panel to the right. Gray reference lines come from the mean selected crop for the active `(IB, istp)`, while themed lines show the active selected crop. The control area at `fig[2,2]` now has one row of left/right button groups for `IB`, `istp`, and `profile` index; the third index wraps over `dens_src_core[IB, istp]`. Verified parsing, checked `mean(Vector{Matrix})` behavior for the reference image, and ran `git diff --check` with only a line-ending warning. Did not launch the live GL window in this non-interactive run.
- Request: Combine the source-read preparation and live source-profile inspector into `script/anlz_ssntfr_phase_distro.jl`, remove `_src` suffixes from the preparation variables, keep `path_output`, avoid adding phase-distribution plotting or phase extraction yet, make the inspector work on the newly prepared `dens_core`, and remove duplicate/leftover configuration after profile calculations were removed.
  Brief: Reworked `script/anlz_ssntfr_phase_distro.jl` into a neutral preparation-plus-inspector workflow: raw densities load into `dens_raw_fmt`, valid duet selection builds `dens_core`, derived mean variables use unsuffixed names, and `draw_profile_inspector!` validates, displays, cycles, and computes gray mean references from `dens_core`. Removed copied comparison/table/phase additions plus leftover `x_modl`/`step_modl` configuration and the duplicate `ylims_profile` function default while keeping `path_output`. Verified the script parses, has no stale `dens_src_core`/`smwh_src`/phase-extraction references, and runs successfully with the WindowsApps Julia launcher outside the sandbox; accepted duet counts matched `[82, 78, 75, 72, 80, 75, 83, 79, 80, 66, 79, 69, 77, 75, 72, 75, 76]`.
- Request: In `script/anlz_ssntfr_phase_distro.jl`, after acquiring `dens_core`, fit every valid `(IB, istp, rep)` crop with the local `modl_peak_1d` model on the centered mean strip over `[-x_max_fit, x_max_fit]`, storing fitted parameters and uncertainty estimates without adding a wrapper function.
  Brief: Added explicit peak-fit configuration and an inline post-`dens_core` loop. The loop builds `ids_rep_valid`, extracts `profile = vec(mean(@view(dens2d[idxs_center, :]); dims=1))`, fits `modl_peak_1d` with bounded LsqFit initial conditions, and stores a vector of named tuples in `fit_peak[idx_IB, idx_istp]` containing original raw `idx_rep`, success flag, `params`, `param_err`, full profile, fitted curve, and residuals. Singular covariance from `stderror` now leaves the fit successful while setting `param_err` to `NaN`. Verified parsing and ran the script with WindowsApps Julia outside the sandbox; it fit all `2586` selected crops, with singular error estimates for `70` crops, and exited cleanly after the inspector path.
- Request: Plot the fitted `modl_peak_1d` result for the selected `(IB, istp, rep)` in the bottom row-profile panel of the live `fig_live` inspector, using the common green fit color and displaying fitted parameters on the same axis.
  Brief: Extended `draw_profile_inspector!` to accept `fit_peak` alongside `dens_core`, validate matching shapes/lengths, and create observables for the selected fitted curve and parameter text. The row-profile axis now overlays the selected fit in `RGBAf(Oklch(0.60, 0.17, 145), 0.95)` and displays `A`, `σ`, `η`, `λ`, and `φ`; both update when IB, istp, rep, or click-selected cuts change. Verified parsing and reran the script with WindowsApps Julia outside the sandbox through loading, selection, all `2586` fits, and live figure construction; stopped the verification process after confirming no inspector error.
- Request: Replace the inspector column-profile panel with a polar scatter of fitted `φ` and `λ` for all valid reps in the active `(IB, istp)`, color points by rep hue and `η`-scaled chroma/lightness, highlight the selected rep by marker size, remove the `x_col` line/column shading, show the `x_max_fit` fit range on the heatmap, and restrict the plotted fit curve to that same range.
  Brief: Added top-level polar-plot knobs `eta_color_max`, `eta_chroma_max`, `eta_lightness_max`, `markersize_fit`, and `markersize_fit_selected`. Replaced `fig[1, 2]` with a `PolarAxis` fed by observables for all successful `fit_peak` entries at the selected `(IB, istp)`, using `φ` as angle and `λ` as radius; point hue comes from original rep id, and `η` controls chroma/lightness. Removed `x_col` state, the heatmap vertical cut line, and column-profile drawing; the heatmap `vspan!` now marks `[-x_max_fit, x_max_fit]`, and the green row-profile fit line is drawn only over that mask. Verified parsing and ran the script through data load, valid selection, `2586` fits, and live figure construction with WindowsApps Julia outside the sandbox.
- Request: Adjust the polar fit scatter so radial distance is `η` rather than `A`, and make the `λ` hue scheme put `6 μm` on the red side while retaining the alternate rep-fraction hue scheme.
  Brief: Changed the polar scatter radius to `params[3]` (`η`) and updated the title/observable names to `φ, η`. Replaced the prior `η`-scaled lightness/chroma controls with constant `polar_lightness = 0.74` and `polar_chroma = 0.12`; hue can still be toggled by the bottom-right button between `:lambda` and `:rep`. The `:lambda` mapping now uses `lambda_hue_span * (1 - lambda_norm)`, so `λ = 6 μm` maps to hue `0` and `λ = 4 μm` maps to the far side of the configured hue span. Verified the script parses.
- Request: In `script/anlz_ssntfr_phase_distro.jl`, update the locally edited peak fitting after adding the background parameter, and for each crop try `phi_peak_init = 0` and `pi`, keeping the fit with smaller residual.
  Brief: Fixed the peak-fit initializer to match the six-parameter `modl_peak_1d(A, σ, η, λ, φ, bg)`, changed `phi_peak_init` into the two-start tuple `(0.0, pi)`, and made the inline fitting loop collect successful trial fits, compare `sum(abs2, resid)`, and store the winning parameters, errors, fitted curve, residual, RSS, and chosen phase seed. Updated the live row-profile parameter text to include `bg`. Verified parsing and ran `script/anlz_ssntfr_phase_distro.jl` with WindowsApps Julia; it loaded the external data, reproduced the valid duet counts, and fit all `2586` selected crops with `6` singular error estimates.
- Request: Update `script/anlz_ssntfr_phase_distro.jl` for the new two-step profile model: use all repeats instead of number/center selection, fit a Gaussian-plus-background first, fit the residual modulation second, show both fit stages in separate inspector rows, add an `xy_center` trace panel, and save reloadable numeric/fitting data.
  Brief: Removed the active number/center rejection by setting `mask_valid_duet = trues(n_IB, n_rep)` while preserving the center-finding pass for diagnostics. Replaced the old single peak fit with an inline two-step loop: bounded `gauss_1d_model` fit on the center-strip mean profile, residual construction as `profile - gaussian`, then two-start bounded `modl_vary_1d_model` fitting on the residual with the lower RSS phase seed selected. `fit_peak` now stores nested `fit_gauss` and `fit_modl` results plus residual profiles. The live inspector now uses `fig[2,1]` for data plus Gaussian fit, `fig[3,1]` for residual plus modulation fit, `fig[2,2]` for controls, and `fig[3,2]` for the current `(IB, istp)` `xy_center` x-coordinate trace with a selected-rep vline; the polar panel reads phase/lambda/amplitude from the modulation fit. Added JLD2 output `SSNTFR_phase_distro_fit.jld2` containing config, numeric arrays, cropped densities, means, centers, masks, and fit results. Verified parsing and ran the script successfully with WindowsApps Julia; it used all `100` repeats for every IB, fit `3400` crops, reported `2` singular error estimates, and wrote the JLD2 file.
- Request: Change the phase-distro inspector `x_center` trace from raw pixels to micrometers without running the full script.
  Brief: Added `x_center_px0` as an inspector/config value derived from the raw image center, converted `xy_center[..., 1]` to `(x_center - x_center_px0) * step_dens`, and changed the center-trace axis label to `x center (μm)`. Included `x_center_px0` in the saved fit config and passed it into `draw_profile_inspector!`. Verified the script parses; per request, did not run the full analysis.
- Request: Add a static phase-distro figure table like `anlz_ssntfr_2d.jl`, with IB rows, neighboring istp columns, mean-density panels with fit/profile shading, polar fit distributions colored by lambda and by rep, and a combined x-center trace column; save as PNG and PDF.
  Brief: Added `draw_phase_distro_table!` to `script/anlz_ssntfr_phase_distro.jl` and imported `CairoMakie` for static output. The table lays out density columns for both istps, polar-λ columns for both istps, polar-rep columns for both istps, and a final combined x-center-in-μm trace column with both istps in theme colors and mirrored IB labels. Density panels use theme colormaps plus the same horizontal profile-collection strip and vertical fit-range shade as the interactive inspector; polar panels use modulation-fit phase as angle and modulation amplitude as radius. The script now saves `SSNTFR_phase_distro_table.png` and `.pdf` under `36.PhaseDistro`. Verified parsing and ran the full script successfully; outputs were written alongside the existing JLD2 fit data.
- Request: Enlarge the static phase-distro figure table rows to interactive scale and let the user run it.
  Brief: Changed the table layout constants so every IB row is `Fixed(360)`, matching the interactive heatmap row height. Density columns are now `Fixed(360)`, polar columns `Fixed(300)`, and the x-center column `Fixed(360)`, making the saved PNG/PDF much larger and easier to inspect. Verified parsing only; per request, did not run the script.
- Request: Add phase-distro options for common per-condition `xy_center`, an offset y-strip center in micrometers, store those configurations, and copy the script to the output folder whenever it runs.
  Brief: Added top-level `use_common_xy_center` and `y_strip_offset` configs. When common centering is enabled, the detected centers are averaged per `(IB, istp)` and repeated along the rep axis so `xy_center` keeps shape `(IB, istp, rep)`. Replaced the hard-coded zero-centered profile strip in fitting, the live inspector, and the static table with an offset-based strip centered at the closest density y coordinate to `y_strip_offset`; this offset affects the profile-collection strip, not the crop center. Added both configs to `fit_config` and copied `script/anlz_ssntfr_phase_distro.jl` into `path_output` at the output stage. Verified parsing and `git diff --check`; did not run the full script.
- Request: Add an `x_fit_offset`, avoid deriving y-strip limits through x-strip variables, and define `y_dens` for non-square density cores.
  Brief: Added top-level `x_fit_offset` and changed fit masks/shaded fit spans to use `abs(x_dens - x_fit_offset) <= x_max_fit`. Split coordinates into `x_dens` and `y_dens`, updated live/static drawing and dimension checks to use `(length(y_dens), length(x_dens))`, and made row/strip selection use `y_dens` while profile/fitting curves remain on `x_dens`. Replaced the old `x_strip_min/x_strip_max` reuse with direct `y_strip_min/y_strip_max` calculations, saved `y_dens` and `x_fit_offset` in the JLD2/config payload, and verified parsing plus `git diff --check`; did not run the full script.
- Request: Add relative RSS for the modulation fit as `norm(residuals(fit)) / max(norm(profile_modl), eps(Float64))`, store it, and display it with the modulation parameters.
  Brief: Imported `LinearAlgebra.norm` and `LsqFit.residuals`, computed `rss_rel_modl` after selecting the winning modulation fit, stored it as `fit_modl.rss_rel` with `NaN` on failure, and appended it to the live modulation fit text. Verified parsing and `git diff --check`; did not run the full script.
- Request: Extend `use_common_xy_center` from a boolean to `:free`, `:fixed_x`, and `:fixed_xy`, where `:free` is the old `false`, `:fixed_xy` is the old `true`, and `:fixed_x` fixes x only while leaving fitted y centers unchanged.
  Brief: Changed `use_common_xy_center` to a symbol config with validation. `:free` leaves all fitted `(x, y)` centers untouched, `:fixed_xy` averages both fitted coordinates per `(IB, istp)` and repeats them across reps, and `:fixed_x` averages only x per `(IB, istp)` while preserving each rep's fitted y center. `y_strip_offset` is independent of these crop centers and only controls where the profile-collection strip is taken inside the cropped image. Verified parsing and `git diff --check`; did not run the full script.
- Request: Add an option for polar fit points to fade by modulation `rss_rel`, with configurable start/end ramp points and simultaneous lightness, chroma, and alpha interpolation.
  Brief: Added top-level polar quality knobs: `polar_alpha`, bad-fit targets `polar_lightness_rss_bad = 0.92`, `polar_chroma_rss_bad = 0.04`, `polar_alpha_rss_bad = 0.1`, and `rss_rel_ramp` for the start/end ramp. Both live and static polar plot color builders now clamp `rss_rel` into that ramp and linearly interpolate lightness/chroma/alpha from the base polar color toward the bad-fit color. Stored these knobs in `fit_config`. Verified parsing and `git diff --check`; did not run the full script.
- Request: Split the phase-distro fitting window into separate Gaussian peak and modulation ranges, show both ranges in plots, add an outer constant-radius phase ring to polar plots, and tighten parameter text formatting/placement.
  Brief: Replaced the single `x_max_fit` with `x_max_fit_peak = 10` and `x_max_fit_modl = 3`, using the wider range for Gaussian fitting and residual display while fitting modulation only on the central modulation range. Live and table density panels now draw the weak peak-range and stronger modulation-range shades before the heatmap so the shades stay underneath. The polar plots now use one `PolarAxis` per panel with an expanded radial limit: fitted points use their modulation radius, outer dots sit at `1.08 * radius_max`, and angle labels sit farther out at `1.20 * radius_max`; live polar cells are `380` wide and static table polar columns are `340` wide. Set radial tick labels explicitly to `0, 2, 4, 6` to avoid long float labels. Reformatted live fit text in Consolas with fixed decimals, split modulation text into left/right/bottom placements, and saved both fit-range configs. Verified parse-only with Julia 1.12.6 and `git diff --check`; a tiny CairoMakie rendering smoke test was blocked by the local `longpath: Access is denied` package-load issue, so the full analysis/render was not run.
- Request: Adapt phase-distro fitting and plots after extending the Gaussian peak model with fitted `x0`, add `φ0`/peak-referenced `φp` phase views, and replace the final x-center trace with fitted `x0`.
  Brief: Added `x0_gauss_init = 0.0` and expanded Gaussian bounds to `[A, x0, σ, bg]` with `x0` constrained to `[-10, 10]`; the fitting loop now seeds the four-parameter Gaussian and displays `x0` in the Gaussian text. Fit storage now converts parameter vectors into named tuples, using Gaussian fields `A, x0, σ, bg` and modulation fields `M, a, b, λ, φ`, and all downstream plot/phase reads use named fields instead of positional indices. Added `φp = φ0 - 2π*x0/λ` helpers for live and static polar plots. The live inspector now has a `phase: :phi0/:phip` button and an `IntervalSlider` for `rss_rel_ramp`, rebuilding polar point colors as the ramp changes. Renamed the live rep-trace observable/helper from `xy_center` wording to `x0_peak` and added a fit-colored dashed vline on the residual modulation panel to mark the same `x0` reference used for `φp`. Added `cohr` moment overlays to the live and static polar plots: each `(IB, istp)` coherence moment is drawn as a black diamond at `moment_angel` and globally scaled `moment_length`, and `cohr` is saved in the JLD2 bundle. The static table now uses nested outer property columns with inner isotope columns: density, `φ0` λ hue, `φ0` rep hue, a body-spanning black `Box` separator in a `Fixed(2)` column, `φp` λ hue, `φp` rep hue, one overlaid duet fitted-`x0` trace instead of separated isotope plots, and an overlaid 162/164 λ histogram bounded by the modulation fit λ range. Polar per-row labels now show only `IB=... A` once per 162/164 duet, and the main table row gap is zero. Fixed GridLayout runtime errors by avoiding indexed row/column sizing before nested grid rows/columns exist. Stored `x0_gauss_init`, `phase_mode`, and tuple `rss_rel_ramp` in `fit_config`. Verified by running the full script successfully with WindowsApps Julia before the final x0-layout tweak; after the tweak, parse check and `git diff --check` passed with only CRLF warnings.
- Request: In the interactive phase-distro inspector, split the density panel vertically and add a lower panel showing the shifted 2D Fourier density of the current density ROI, then extend FT displays to include per-condition mean complex and mean-absolute FT maps in the live figure and static table.
  Brief: Added `FFTW` imports and moved FT calculation outside visualization: after `dens_core` is available, the script uses `smwh_dens_ft = (80, 80)` to select a centered `161 x 161` patch around `(x_dens, y_dens) = (0, 0)`, multiplies it by the outer product of two `tukey1d` windows, stores that payload as `dens_core_ft_masked`, computes the complex `fftshift(fft(...))`, splits per-shot FT into `dens_core_ft_amp`/`dens_core_ft_phs`, and computes 2D per-condition `dens_core_ft_cmpx_mean` and `dens_core_ft_absl_mean`. The live figure now restores the main density heatmap to `fig[1,1]` and adds a dedicated FT row containing masked density, per-shot complex FT, mean complex FT, a phase-amplitude shader legend, mean absolute FT, and a mean-absolute scale strip; controls were split into cycling and tuning subcolumns. The static table now includes mean complex FT and mean absolute FT groups before the right-side IB labels, each with isotope panels and explicit colorbar/legend panels. Saved the masked density, amplitude/phase arrays, mean FT arrays, and axes in the JLD2 bundle. Fixed runtime issues surfaced by testing: `:free` center mode now rounds tuple elements correctly, mean FT maps now reduce matrices elementwise instead of producing vectors, and FT shader normalization guards against zero amplitudes. Verified parsing and `git diff --check`, then ran the full script with WindowsApps Julia to completion under `40.MeanAbsl2D`; all 3400 fits completed, the JLD2 file saved, and the expanded table PNG/PDF path was generated.
- Request: Add an amplitude-only view for the complex mean FT2D output: a new table column between the colorful complex FT and mean absolute FT columns, and an interactive toggle that switches the complex FT panels and colorbar between complex phase-colored and amplitude-only rendering.
  Brief: Added paired Makie heatmap layers in the live FT row so the shot FT and mean complex FT axes can toggle between RGBA complex rendering and isotope-colormap amplitude rendering via a new `FT: complex/amp` button; the shader/scale panel switches with the same observable. Added a new `mean FT amp` nested isotope column in the static table between `mean FT complex` and `mean |FT|`, plotting `abs.(dens_core_ft_cmpx_mean)` with the same isotope-themed amplitude color scale and no additional colorbar. Restored a fallback `cohr` summary from fitted modulation phases/amplitudes when an external `cohr` payload is not defined, keeping the existing moment markers and save list runnable. Verified parse and `git diff --check`, then ran `script/anlz_ssntfr_phase_distro.jl` with WindowsApps Julia; all 3400 fits completed, JLD2 saved, and updated table/coherence PNG outputs were generated under `40.MeanAbsl2D`.
- Request: Refactor the new `script/anlz_ssntfr_ft2d.jl` so its saved data and visualizations contain only the FT2D workflow, with density/FT/profile table panels and a simplified interactive FT inspector.
  Brief: Replaced the stale phase-distro save and plotting tail with FT-only configuration/data saving, copying the script into the output folder and saving `SSNTFR_ft2d_fit.jld2`. The table now has mean density, `|ft2d_cmpx_mean|` plus coherent 1D profile, and `ft2d_absl_mean` plus incoherent 1D profile columns for both isotopes, with aligned x axes, requested profile limits, isotope color maps, complex shader legend, and absolute-value color scales. The live figure now has density and per-shot complex FT on row 1; mean complex FT plus shader colorbar and controls on row 2; amplitude/coherent-profile and absolute/incoherent-profile panels with colorbars on row 3. Removed phase-fit names and stale `dens_core_ft_*` references, retained tunable FT/color/profile settings, and stored `ft2d_cmpx`, both mean maps, profiles, cropped densities, centers, and coordinates. Verified parsing, `git diff --check`, and a full Julia run; the JLD2, PNG, and PDF outputs were generated successfully under `44.MeanAbsl2D`.
- Request: Repair the FT2D table sizing, restore dynamic per-shot complex-FT color normalization from the old script, add backward/forward controls for IB/istp/rep, use isotope theme colors for profiles, and consolidate duplicate shading helpers.
  Brief: Added explicit nested table row/column sizes after all nested axes are created, with separate two-row density sizing and three-row FT/profile sizing. Unified the static/live complex color rendering into one `gen_ft_rgba(ft; rng_amp=...)`; the table and mean maps use the configured range while each per-shot FT uses its own side-peak maximum. Replaced one-way selectors with paired previous/next buttons for IB, isotope, and rep, and made live profile colors reactive to the selected isotope. Fixed Makie invalid-row sizing errors surfaced during testing. Verified parsing, `git diff --check`, and a complete Julia run with successful JLD2, PNG, and PDF generation.
- Request: Add reconstructed-envelope tail removal to the FT2D workflow, then support a sidepeak-mask alternative with masked FT1D profiles, direct reference subtraction, amplitude heatmap overlays, unmasked normalization, and configurable modulation-band markers.
  Brief: Added the two-stage `double_gaussian_disk_2d_model_abrr` envelope fit using the existing tail-initialized 1D guess and fixed `x0`, `y0`, and `skew_x` at zero. Factored density-to-complex-FT and absolute-FT-to-one-sided-profile processing into shared `calc_ft2d_cmpx` and `calc_prfl_modl1d` helpers used by data and reconstructed envelopes. Added reconstructed reference profiles, standard center-scaled `calc_modl_tail`, and no-scaling `calc_modl_tail_masked`; the new `use_mask_sidepeak = true` path uses the latter and saves masked/tailless outputs. Migrated `to_masked_clr` and sidepeak connected-component masking from `test_mask_sidepeak.jl`, overlaying grayscale complement and isotope-colored masked components on static/live amplitude heatmaps. Added `ky_max_modl` with dashed `±ky_max_modl` lines and saved all new settings/data. Used output title `45.MeanAbsl2D.Mask`; full Julia run completed successfully and regenerated the JLD2, PNG, and PDF outputs.
- Request: Keep the full positive-kx/full-ky FT calculation while restricting all visual FT maps and profiles to `kx=(0, 0.5)` and `ky=(-0.2, 0.2)`, include complete border pixels, combine last-row tailess profiles, and reproduce stacked profile heatmaps for tailed/unmasked and masked/tailess data.
  Brief: Changed the full-ky calculation mask to a Boolean all-true mask and added half-pixel-expanded plotting masks/limits for the requested FT viewport; saved the viewport configuration. Added a combined last-IB static summary with coherent solid and incoherent dashed tailess curves for each isotope, plus two saved stacked-profile figures for tailed/unmasked and masked/tailess profiles. Fixed a stale output filename and validated the full `anlz_ssntfr_ft2d.jl` run successfully, including JLD2, PNG, and PDF exports under `46.MeanAbsl2D.Mask`.
- Request: Put the coherent/incoherent tailess overlay in the final static-table column for every IB row and orient the stacked IB-profile heatmaps outward from the central zero, using the masked-tailess color scale for both variants.
  Brief: The final table column now shows the corresponding coherent solid and incoherent dashed tailess profiles for each IB/isotope pair, with the panel labeled `tailess`. Stacked profile heatmaps now reverse the first isotope’s x-axis and use outward wavenumber ticks; tailed/unmasked and masked/tailess figures share color ranges derived from the masked-tailess profiles, preserving side-peak visibility. Parse check, `git diff --check`, and the full Julia run all passed.
- Request: Put the tailess coherent/incoherent overlays in a separate final table column, restore the original incoherent column, and match the older script’s profile-axis formatting.
  Brief: Added a fifth nested table column containing only two isotope profile axes per IB, with coherent tailess solid and incoherent tailess dashed lines. Restored the fourth column to its ordinary incoherent heatmap and profile, added compact wavenumber ticks and side y-axis placement for stacked heatmaps, and retained border-inclusive FT heatmap limits. Parse, diff checks, and the regenerated table/stacked exports completed successfully.
