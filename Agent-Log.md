- Request: Build a PCA helper in `src/corr.jl` for serialized image-like samples: accept an integer `n_mode` and an arbitrary-dimensional array whose elements are arbitrary-dimensional real arrays; return `n_mode` modes, each with a reshaped mode profile and a per-sample weight array matching the outer sample shape. Before implementation, compare using a custom `ModeWeight` type versus a `Dict`.
  Brief: Explained that `ModeWeight` gives clearer fields, type-stable downstream use, and a stronger API contract, while `Dict` is more flexible but typo-prone and less compiler-friendly. Added `ModeWeight`, `build_pca_matrix`, and `fit_pca_modes`; the wrapper flattens each inner sample as one PCA observation column, calls `MultivariateStats.fit(PCA, ...)`, uses PCA projections as profiles, uses `predict` for per-sample weights, and reshapes outputs back to their original profile/sample axes. Parsed `src/corr.jl` successfully; full runtime verification was blocked because this Julia install cannot load standard libraries such as `Pkg`/`SparseArrays`.
- Request: Refactor `script/anlz_miscibility.jl` so each `runinfo` reads all corresponding folders as one raw data block, formats by variables recorded together in a named tuple, removes `rep_each`, keeps number-stat plotting but comments out the full duet table, and makes `_fmt` data align with variable axes rather than appending image dimensions.
  Brief: Updated `runinfos` to use `vars=(IB=..., rep=..., bias=..., t_hold=..., istp=...)`, infer variable names and dimensions from `runinfo.vars`, validate loaded shot count against `prod(n_dim_vars)`, and format `dens_full_fmt` as an n-d array over variables whose elements are cropped image matrices. Changed number statistics to use `sum.(r.dens_full_fmt)`, set `title_anlz` to `[05.15].04.DevTest`, and commented out `essn_2d_fmt`/`info_fmt` generation plus the `fig_full_duets` plotting block. Verified the selected runinfo prints `dens_full_fmt size: (1, 5, 11, 11, 2)` and `image size: (61, 61)`.
- Request: In `persolo.jl`, add named arguments to `fit_prfl_modl_twinpeak_decay_1d` and `fit_prfl_modl_twinpeak_1d` for each fitting parameter, using the current `p_init`, `p_upper`, and `p_lower` defaults and following the `fit_dens2d_gaussian_elliptic_disk` hint style.
  Brief: Added `(max, min, init)` named-tuple keyword hints for all fit parameters: `M_hint`, `σ0_hint`, `P_hint`, `σ_hint`, `p_hint`, plus `D_hint` and `λ_hint` for the decay model. Both functions now build the LsqFit initial, upper, and lower parameter vectors from those hints. Verified `src/persolo.jl` parses with Julia 1.12.6; the WindowsApps launcher path failed to start in this shell, so verification used the installed Julia binary.
- Request: Find more hard-coded values in `persolo.jl`, `percond.jl`, `corr.jl`, and the main analysis scripts that may belong in configurable structures or default arguments, and append them below the existing `Configurables.md` notes.
  Brief: Appended candidate configurables to `Configurables.md`, then reorganized the list by topic after the user commented out some lower-priority items. Current sections cover run/data selection, geometry/calibration, solo extraction, profile/envelope fit hints, PCA/trend analysis, and stacking behavior; plot-related candidates are omitted and commented-out candidates are preserved in a final HTML comment block. This was a markdown-only inventory pass; no Julia runtime verification was needed.
- Request: Split miscibility analysis into a new runner plus called per-run script; move setup/config into the runner, keep generated analysis values accessible after each include without `global` keywords in `anlz_miscibility.jl`, add missing configurable helper arguments from `Configurables.md`, copy the runner and called script to the target folder, use title `[05.26].06.DevTest`, and test only currently active analysis functionality.
  Brief: Added `script/anlz_miscibility_runner.jl` with library loading, includes, runinfo candidates, `ids_runinfo`, paths, target creation, provenance copies, and active analysis configurables; kept concise size-plot styling literals inside `script/anlz_miscibility.jl` rather than naming them as runner config. Reworked `script/anlz_miscibility.jl` into a per-run body with no `using`/`include` setup, no enclosing runinfo loop, and no `global` keywords for generated values such as `r`, `xy_peak_duet`, `essn_2d_fmt`, `extr_2d_fmt`, `info_fmt`, `num_fmt`, `stat_n_fmt`, `essn_2d_stacked_over_rep`, and `info_stacked_over_rep`. Added/pass-through configurable keywords in helpers: `format_dens_runinfo(...; len_avg_peak=10)`, `fit_dens2d_gaussian_round_disk(...; A_hint=...)`, `calc_solo_extr(...; selector_moment, selector_sidepeak, fit_tailess_kwargs, fit_asymm_kwargs, fit_round_kwargs)`, and `query_weight(...; scaling=1000.0)` with `anlz_trend_from_extr(...; query_weight_kwargs=...)`; also fixed the blocking `AbstractMatrixr` typo in `find_positive_cluster_center`. Verified syntax for touched files, ran an include-scope smoke test, and successfully ran the active-path miscibility runner with WindowsApps Julia. Output folder `...\SingDrplMisc\AnlzRoutine\[05.26].06.DevTest` contains copied scripts plus six active size plot PNGs for run sets 71-75, 76-80, and 81-82. The installed Julia 1.12.6 binary still cannot load stdlibs/packages such as `Pkg`/`UUIDs`, so full execution used the WindowsApps launcher.
- Request: Move common solo-correlation routine configuration into `script/anlz_routine_batch.jl`, format each runinfo like the miscibility runner with variables stored in `runinfo.vars`, allow expanded formatted axes, and keep active `script/anlz_routine.jl` function calls aligned with current helpers.
  Brief: Reworked `script/anlz_routine_batch.jl` as the setup/config runner with provenance copies, shared geometry, selectors, fitting/PCA/trend options, `ids_runinfo = 1:5` for testing, and runinfos shaped as `vars=(IB=..., rep=..., t_hold=..., istp)`. Trimmed `script/anlz_routine.jl` to a per-run analysis body that unpacks `format_dens_runinfo` into local variables, uses `val_vars` for variable values, formats full variable axes, keeps `n_dim_vars` as the full variable shape, and uses `n_dim_vars_per_ib` after selecting the configured single-IB slice for the existing `(rep, t_hold, istp)` plotters; applied the same formatter unpacking and `val_vars` naming to `script/anlz_miscibility.jl`. Added stage timing/progress messages and updated active calls to `format_dens_runinfo`, `calc_solo_essn_2d`, `calc_solo_extr`, `fit_prfl_modl_twinpeak_decay_1d`, `fit_pca_modes`, and `anlz_trend_from_extr` with runner-provided keyword configs. Updated `gen_run_tag` to read singleton `IB` values from `runinfo.vars`, fixed `anlz_trend_from_extr` moment access through `SoloExtract.sidepeak`, and fixed `calc_stacked_essn` by keeping original array/scalar `mean(map(...))` style while replacing slow `mean.(zip(ts...))` tuple averaging with index-wise tuple means. Verified parsing of touched Julia files and ran the narrowed routine batch `ids_runinfo = 1:5` to completion without advancing titles; all five selected runs completed. Attempted `script/anlz_miscibility_runner.jl`, but the WindowsApps Julia launcher failed to start with a logon-session `ResourceUnavailable` error and the installed Julia binary failed during project/package loading with `SystemError: longpath: Access is denied`.
- Request: Rename the excitation analysis scripts from the old routine names to `anlz_excitation.jl` and `anlz_excitation_runner.jl`, and document the name change without changing analysis titles.
  Brief: Renamed `script/anlz_routine.jl` to `script/anlz_excitation.jl` and `script/anlz_routine_batch.jl` to `script/anlz_excitation_runner.jl`. Updated the runner include/provenance-copy path variable to `path_anlz_excitation` and left the existing output folder/title behavior unchanged. Added a name-change notice plus updated excitation workflow wording in `Configurables.md`. Verified the renamed scripts parse with Julia.
- Request: Combine excitation runinfos by `tag_head` so the runner has one CFNM group and one NTRC group, represent the IB axis as `runid_IB=(runid, IB)` tuples, keep `_fmt` outputs aligned with that first axis, and make plotting/save tags run per IB; keep raw-data loading keyed by explicit `(date, runid)` metadata instead of analysis variable names.
  Brief: Reworked `script/anlz_excitation_runner.jl` to contain two grouped runinfos, with `date_runid=(date, runid)` as the IO source of truth and `runid_IB` as the first formatted analysis axis. Updated `format_dens_runinfo` and `gen_run_tag` in `src/loadfmt.jl` to load grouped data from `date_runid`, validate that its length matches the first variable axis, preserve singleton per-IB tags, and keep range run-id formatting compatible. Reworked `script/anlz_excitation.jl` so `dens_full_fmt`, `essn_2d_fmt`, `essn_stacked_over_rep`, `essn_stacked_over_rep_t`, fits, extracts, PCA modes, and trend outputs retain a leading `runid_IB` axis; `xy_peak_px` is found once from the combined group mean; per-shot info/log text extracts `runid` and `IB` from `runid_IB`; per-IB plot runinfos extract date/runid from `date_runid`; and trend/PCA plotting loops per IB with per-IB filenames. Updated `Configurables.md`; verified touched Julia files parse.
- Request: Replace the excitation `runid_IB` tuple variable with plain `vars.IB` values while preserving the name-value variable pattern, and add `runinfo.bind_id=:IB` to declare that the run IDs are bound one-to-one to the IB axis.
  Brief: Updated `script/anlz_excitation_runner.jl` so grouped excitation runinfos use `vars=(IB=..., rep=..., t_hold=..., istp)` plus `bind_id=:IB`, leaving `date_runid` as the source of date/runid metadata. Added `validate_bind_id` in `src/loadfmt.jl`; when `bind_id` is present it verifies that the named variable exists in `runinfo.vars` and has the same number of values as the run IDs. Updated `script/anlz_excitation.jl` so data processing, labels, and logs refer to the first axis as `IB`, while per-IB filenames still combine `runid` from `date_runid[c]` with `IB` from `val_vars.IB[c]`. Updated `Configurables.md`; verified the touched Julia files parse and ran a tiny Julia snippet confirming valid bindings pass while mismatched runid/IB counts throw `DimensionMismatch`.
- Request: Add a one-folder-per-runinfo version of excitation `runinfos` for performance comparison, while keeping the current grouped version available but commented out.
  Brief: Made the active `runinfos` in `script/anlz_excitation_runner.jl` list each excitation folder separately again, using scalar `date`, `runid`, `tag_head`, `bind_id=:IB`, and `vars=(IB=..., rep=..., t_hold=..., istp)` entries. Kept the grouped CFNM/NTRC `date_runid` version immediately below as a commented alternative so it can be restored for comparison. Preserved the current title `[05.26].53.DevTests` and trend selector settings. Verified the runner parses.
- Request: Convert fitting-result dictionaries to named tuples, update all downstream users while leaving axes-access dictionaries alone, test with one IB-separated excitation run, then restore the full grouped excitation runinfos.
  Brief: Changed the fitting/moment result helpers in `src/persolo.jl` to return named tuples and updated `SoloSidepeak`, `SoloEnvelope`, `calc_solo_extr`, trend extraction, solo visualization, and miscibility consumers to use dot access for fit parameters, moments, fit functions, and residuals. Left plot/axis/info dictionaries intact. Split excitation runner alternatives into `runinfos_separated` and `runinfos_grouped`; verified the separated `ids_runinfo = 1:1` path ran successfully through loading, extraction, PCA, and trend analysis, then restored `runinfos = runinfos_grouped` with `ids_runinfo = eachindex(runinfos)`. Parsed all touched Julia files and searched for leftover fitting-result string-key access.
- Request: Add runner-configured `sel_vars` selectors that filter formatted variable axes by index predicates/ranges, value predicates/sets, or both, while still loading the full raw data described by `runinfo.vars`; expose selected `val_vars` and matching `_fmt` outputs to the analysis scripts, and preserve the full unfiltered variable values as `val_vars_full`.
  Brief: Added selector helpers in `src/loadfmt.jl` and a `sel_vars` keyword to `format_dens_runinfo`; the formatter now loads and reshapes the full data first, then filters `val_vars`, `dens_full_fmt`, `n_dim_vars`, and returned `runinfo.vars` by selected axes while returning the original values as `val_vars_full` and `runinfo.vars_full`. Selection supports `(; var=(; index=..., val=...))`, index ranges/vectors/masks/functions, and direct value filters such as scalars, ranges, vectors, masks, or predicates. Bound metadata such as excitation `date_runid`/`runids` is trimmed when `runinfo.bind_id` is selected, so per-axis run metadata stays aligned. Added default `sel_vars = NamedTuple()` placeholders in both runners and passed it through the excitation and miscibility scripts. Verified `git diff --check`, a Base-only Julia selector snippet, and a narrowed real miscibility run with `sel_vars = (; t_hold=t -> (t < 80 && t > 20))`; that run loaded set 1 and reported `t_hold = 8` with `dens_full_fmt size: (1, 5, 11, 8, 2)`.
- Request: Implement `corr.jl/mean_dict` so it returns a dictionary whose selected keys come from an array of dictionaries, filtered by inclusive and exclusive key patterns, and whose values are the means of the corresponding values across the dictionary array.
  Brief: Added `Statistics.mean`, a small pattern-matching helper that accepts single or grouped string/regex patterns, and implemented `mean_dict` for `AbstractArray{<:AbstractDict}`. The function rejects empty input, builds the union of keys from all dictionaries, filters by `pattern_incl` and `pattern_excl`, requires each selected key to exist in every dictionary, and returns per-key means. Fixed string patterns to compile as regexes so the defaults `pattern_incl=".*"` and `pattern_excl="(?!)"` work as intended. Verified `src/corr.jl` parses and exercised the filtering/averaging behavior with standalone Julia snippets; full project include verification was blocked because the WindowsApps launcher failed with a logon-session error, escalation review timed out, and the installed Julia binary cannot load stdlibs such as `Statistics`.
- Request: Generalize the excitation trend plotting so each call draws one property variant by convention-matched evolution/frequency dictionary keys; keep per-IB all-property figures, add separate per-property all-IB figures with IB rows and istps side by side, and allow the all-IB axes builder to include any combination of repeat, stacked, or repeat-overlay groups while plotters use only available groups.
  Brief: Added declarative trend property/variant specifications, a generic paired-axis builder with configurable evolution/frequency widths and row height, and `plot_trend_property_variant!` key construction such as `moment-height` to `evol-all-moment-height` plus `freq-sel-moment-height`. Added a density-sum spectrum so configured properties retain evolution/frequency pairs. Reworked `plot_trend_all!` to independently detect and populate optional `repeats`, `stacked`, and `all` axis groups. Added `set_axis_trend_property_IB_istp!` to arrange every IB as a row and each istp as a side-by-side block using runner-configured `trend_all_IB_groups`. Split `script/anlz_excitation.jl` into a per-IB all-property plotting loop and a later per-property all-IB plotting loop; retained compatibility wrappers for existing sidepeak/envelope callers. After comparing against the supplied all-property graph, made overlay evolution placement explicit per property: density sum uses column 1, sidepeak fit/moment use columns 1/2, envelope size uses column 2, and frequency uses column 3. Full narrowed-run testing exposed and fixed two unrelated NaN edge cases: zero spectral power now returns a zero spectrum in `query_weight`, and `gen_clrmap_solo` no longer computes `0/0` when `thres_alpha=0`. Made the per-IB plot timer local to remove a REPL soft-scope warning. Fixed all-IB panel collisions by replacing the flat top-level column layout with one nested `GridLayout` per istp, containing its own repeat/stacked/overlay sublayouts and separator; `set_panel_trend_properties!` now explicitly sizes its rows and evolution/frequency columns so dimensions propagate from axes outward. Added `align_overlay`: all-property figures keep aligned three-column overlay rows, while per-property figures default to compact overlays and allocate the second evolution column only when a variant actually requests it. Thus density sum and envelope size use evolution/frequency pairs, while sidepeak properties retain fit-evolution/moment-evolution/frequency triples. Added a conditional 2-pixel vertical `Box` inside each per-property istp layout when repeat panels coexist with stacked and/or overlay panels, matching the existing per-IB separator; no separator is drawn for repeat-only or processed-only combinations. Verified the separator across five synthetic group combinations. Also ran the narrowed real excitation analysis with an in-memory title override to `[05.28].60.DevTestPropertyTrends`, leaving the runner's updated `title_anlz` untouched; all six per-property all-IB figures were regenerated, and visual checks of envelope size and sidepeak height confirmed the compact/split overlay geometry. The initial bare `Box` depended on Makie's theme defaults and could appear only as a wider gap, so both per-IB and per-property separators now explicitly use a black fill with zero stroke. The real-data wrapper timed out only after its outputs had completed, as confirmed by their timestamps. `src/viscorr.jl` parsed in the earlier verification and `git diff --check` passes; a fresh Julia render after the explicit-color change was blocked by Windows sandbox process startup failure.
- Request: Document that late-stage plotting and data-processing tests should retain processed data in a Julia REPL, then temporarily run the prior narrowed excitation selection under analysis title `.60`, restore the runner, and verify later plotting by reusing those in-memory variables.
  Brief: Added the persistent-REPL reuse guidance to `AGENTS.md`. Temporarily set `script/anlz_excitation_runner.jl` to `[05.28].60.DevTestPropertyTrends` with IB `5.316:5.318` and `t_hold <= 30`, ran one real processing pass, then reran the script from its `# Overall plots` marker in the same Julia process without repeating upstream analysis. Restored the runner to `[05.28].61.DevTestPropertyTrends` and `sel_vars = NamedTuple()` immediately afterward. The plotting-only rerun regenerated all six `.60` per-property images; visual inspection of `sizes` and `height` confirmed clearly visible black separators in both compact and split overlay layouts. `git diff --check` passes.
- Request: In excitation trend analysis, replace the sidepeak/envelope time selector pair with a `selector_t_spectrum` named tuple covering number, sidepeak weight/height/width/wavenum, and envelope size; use those ranges in trend shading; and generalize the envelope-only spectrum-vs-IB plot into spec-driven per-property figures comparing stacked spectra with mean individual-rep spectra.
  Brief: Added selector normalization and per-property masks in `src/corr.jl`, with trend dictionary keys such as `t_vec_sel_number`, `t_vec_sel_sp_weight`, and `t_vec_sel_nvlp_size` while preserving old `t_vec_sel_sp`/`t_vec_sel_nvlp` aliases. Updated `default_trend_property_specs` in `src/viscorr.jl` and made the runner define an explicit editable `trend_property_specs` vector plus `selector_t_spectrum`. Added `set_axis_spectrum_property_IB_istp!`, `calc_freq_IB_matrix`, and `plot_spectrum_property_IB_istp!` so `script/anlz_excitation.jl` now writes separate spectrum-vs-IB figures for every configured property, with istp blocks side by side, stacked vs mean-rep spectra as inner columns, and variants as rows. Preserved the current runner narrowing `ids_runinfo = 1:1`. Verification: `git diff --check` passes; edited Julia files parse with the installed Julia binary; a narrowed real-data probe using WindowsApps Julia processed CFNM IB 5.316/5.318 and t_hold 0-30 ms, generated data once, then reran the spectrum-and-later plotting section from in-memory variables in the same Julia process. Output folder `[06.06].62.CFNM-NTRC-Probe` contains all six `CFNM_spectrum_*_IB` figures plus renamed all-IB trends (`number`, `nvlp-size`), and visual inspection checked envelope-size, weight spectrum, and weight trend layouts.
- Request: Create `script/create_pca_package.jl` to package saved PCA mode images and peak spectra for PicTagger: use one `gen_run_tag` folder per IB, record every mode's IB and frequency/`value_reduced` pairs in sample-compatible `cache.yaml`, retain the sample tag definitions, leave IDs/tags/timestamps empty, and generate the folder without zipping it.
  Brief: Added a configurable standalone exporter that loads `runinfo`, `val_vars`, `n_pca_modes`, and `pca_spectra` from the saved JLD2 file; resolves each IB with the existing bound-runinfo and `gen_run_tag` logic; copies mode images into per-IB folders as `PCA.Mode.NN.png`; and manually emits PicTagger-compatible YAML with `version`, the reference tag list, relative image paths, source metadata, mode indices, and all prominent peak frequency/weight pairs. Ran it successfully on `[06.08].64.PCATests.[t=0-100ms]`, producing `PCA modes PicTaggerCache` with 192 images across 12 IB folders and a spot-checked `cache.yaml`. JLD2 warned while reconstructing saved anonymous/named-tuple types, but the required fields loaded and exported correctly.
- Request: Extend `scritp/compare_selector_t_property.jl` with a standalone late-stage summary pass that discovers saved CFNM/NTRC spectrum images by filename, extracts property plus `t_min`/`t_max`, and writes one large PDF grid per property with `t_min` rows and `t_max` columns; run only the added pass without repeating upstream analysis. Preserve each source image at its original size in the PDF grid.
  Brief: Appended filename parsing, folder collection, and CairoMakie grid-rendering helpers. The pass independently scans `[06.08].66.SelT/CFNM` and `NTRC`, discovers the six properties and selector axes, leaves unavailable selector combinations blank, and targets `<property>_selector_t_summary.pdf` in each group folder. Confirmed both folders contain 91 PNGs for each of `number`, `weight`, `height`, `width`, `wavenum`, and `nvlp-size`. The renderer derives dimensions independently for each group/property, validates every image in the property before placing it, transposes loaded row/column image arrays for upright Makie display, and fixes grid rows/columns to one quarter of the native image dimensions by default through validated `scale_image=0.25`; Makie determines the outer figure size through `resize_to_layout!`. Added console progress at folder scanning, property setup, every `t_min` row, resize/save, and group/final completion stages. The accessible CFNM files confirm `number` is 3384 x 632 while the other five properties are 3384 x 1010. Validated the edited Julia script parses and `git diff --check` passes. A user-started Julia run completed the prior small-cell PDFs for both groups, but rerendering the corrected versions was blocked: sandboxed Julia package loading failed with `longpath: Access is denied`, while both outside-sandbox render approval attempts timed out before launch.
- Request: Integrate the mask-based 1D modulation profile from `script/test_fringe_sidepeak.jl` into the excitation analysis path, with masks configured in the runner, sidepeak-only fit helpers without center-peak parameters, tail fitting over `(IB, t_hold, istp)` using the fully masked profile and `y_modl > 0.2`, updated extraction/visualization, and test until the runner generates results.
  Brief: Added modulation-mask helpers, masked profile normalization, and stored modulation profile data under `SoloEssentials.prfl_modl`, with `main=(; norm, raw, normed_px)`, `side=(; raw, normed_px)`, and `mask=(; fringe, center, side, main)` while keeping `modl2d` as a direct struct field. Added sidepeak-only `fit_prfl_modl_sidepeak_decay_1d` and `fit_prfl_modl_sidepeak_1d`, updated `calc_solo_extr` to use sidepeak-only parameter indices, and updated stacking in `src/percond.jl` to preserve the nested profile tuple. Added `mask_modl` and `selector_tail_sidepeak = y -> y .> 0.2` to `script/anlz_excitation_runner.jl`; wired `script/anlz_excitation.jl` to pass masks, fit per `(IB, t_hold, istp)`, and use `essn.prfl_modl.side.normed_px`. Removed the old `smw_modl`/`smw_ft` strip-width path entirely: `calc_solo_essn_2d` now builds default full masks when no mask tuple is supplied, and excitation, miscibility, and test call sites use the mask/default-mask signature. Updated `src/vissolo.jl` to keep the existing solo layout while drawing transparent FT mask layers, all density heatmaps with `clrmap_dens`, 10 um major and 2 um minor grids on the two core density panels, black 1D profile lines, and a restored upright shaded band from the fitted sidepeak curve; the essn table shows full ROI, unmasked core crop, and core restored from fully masked modulation. Added an empty-selection guard in `src/vispca.jl` so PCA plotting skips the selected-time shading when the runner's `selector_t_pca` selects no times. Verified edited files parse, `git diff --check` passes, `rg` finds no remaining old `SoloEssentials` modulation-profile field access in main code, and the lighter runner selection `(1 IB, 3 reps, 8 t_hold values, 2 istps)` completed with `smwh_roi=(50, 100)` and regenerated `CFNM_5.316_r52_essn_table.pdf` in `[06.20].77.SidePeak2DMask.Dev.Lite`; the later shaded-band-only tweak was not runtime-tested per request. The run produced non-fatal JLD2 warnings about serializing anonymous functions during PCA saving.
- Request: Make excitation processed data cacheable for plotting by moving fit models out of saved fit records, saving selected selector values instead of selector functions, splitting caches into essn/extr and trend files with metadata, then test with one processing REPL and one clean reload/plotting REPL.
  Brief: Added pure model evaluators in `src/fitmodels.jl`, changed `src/persolo.jl` fitting helpers and `SoloExtract` contents so fit records store numeric params/residuals only, materialized cropped density arrays for stable serialization, and updated `src/vissolo.jl`, `src/vispca.jl`, `script/plot_stack_over_rep_t.jl`, and `script/test_fringe_sidepeak.jl` to reconstruct curves from model functions plus params. Added `src/excitation_cache.jl` and `script/replot_excitation_cache.jl`; `script/anlz_excitation.jl` now writes `CFNM_essn_extr.jld2` for `essn_2d_fmt`, stacked essentials, stack fits, `extr_fmt`, and stacked extracts, plus `CFNM_trend.jld2` for trends, profile evolution, PCA modes/spectra, and plotting metadata. The saved fitting metadata records model names and numeric coordinate selections/ranges; PCA spectra now store `val_t_sel`/`mask_evol` instead of `sel_evol`. Verified `git diff --check`, parsed/loaded edited source files with WindowsApps Julia, ran the current lite `script/anlz_excitation_runner.jl` once in a persistent processing REPL, saved both split caches without anonymous-function warnings, then loaded those caches in a separate fresh Julia REPL and regenerated active figures plus `solo_table_cache_probe_[CFNM_5.316_r52].pdf` under `[06.20].81.Dev.Fitting\Reloaded` without raw loading or reprocessing.
- Request: Replace the wrapped excitation cache plotting path with top-level split scripts: extraction/save, load extraction, correlation/save, load correlation, and visualization; make the runner include extraction/correlation/visualization sequentially, add a short rerun script for loading saved data into new output folders, copy relevant scripts into outputs, and test fresh, extraction-load, correlation-load, and extraction-figure rerun cases.
  Brief: Added `script/anlz_excitation_extr.jl`, `load_excitation_extr.jl`, `anlz_excitation_corr.jl`, `load_excitation_corr.jl`, `anlz_excitation_vslz.jl`, and `anlz_excitation_rerun.jl`; made `script/anlz_excitation.jl` a compatibility wrapper around the split stages. Removed the earlier wrapped visualization helper path and changed `script/anlz_excitation_runner.jl` to copy/include the split scripts in order with `plot_extr_figures = false` by default. Extraction now saves `CFNM_essn_extr.jld2`; correlation now saves `CFNM_corr.jld2`; rerun mode `corr_from_extr` copies the extraction cache forward so later extraction visualizations can load from that output. Tested four real lite cases with WindowsApps Julia: fresh split run to `[06.20].81.Dev.Save`, load extraction and recompute/save correlation to `[06.20].82.Dev.Load.Corr`, load correlation and generate correlation figures to `[06.20].83.Dev.Load.Corr.Vslz`, and load extraction from 82 to generate full solo tables in `[06.20].84.Dev.Load.Extr.Vslz`. Verified expected cache/figure files and copied scripts in all four folders; `git diff --check` passes.
- Request: Document the updated excitation routine where normal analysis enters through the runner, while REPL debugging and cache-based reruns enter through the rerun script; leave files unchanged if existing docs are not stale.
  Brief: Updated `AGENTS.md` with the current excitation entry-point habit: use `script/anlz_excitation_runner.jl` for fresh analysis, `script/anlz_excitation_rerun.jl` for REPL debugging/replotting from saved caches, and include split stage/visualization scripts from those entry points. Added the same routine note near the excitation configurables in `Configurables.md`. No Julia execution was needed; this was a documentation-only update.
- Request: In excitation correlation analysis, save both per-repeat and stacked modulation sidepeak profile evolution arrays, rename the per-repeat array to `prfl_evol` and the stacked array to `prfl_evol_stacked`, and move the long profile-evolution visualization block into reusable `viscorr` helpers.
  Brief: Added `prfl_evol` in `script/anlz_excitation_corr.jl` with outer axes `(IB, rep, istp)` and saved the old stacked/mean-over-rep matrix as `prfl_evol_stacked`; `script/load_excitation_corr.jl` now loads the new names while tolerating the temporary `prfl_evol_reps` cache and older stacked-only caches. Added `set_axis_prfl_modl_evol!` and `plot_prfl_modl_evol!` to `src/viscorr.jl`, then reduced `script/anlz_excitation_vslz_corr.jl` to helper calls for repeat columns, separator, stacked column, colorbars, and heatmaps. Verified touched Julia files parse with WindowsApps Julia, ran `script/anlz_excitation_rerun.jl` directly without changing its configured `[06.20].81.Dev.Save` input and `[06.20].86.[←81].Dev.PrflEvol.Lite` output, confirmed `CFNM_corr.jld2` has `prfl_evol` size `(2, 3, 2)`, `prfl_evol_stacked` size `(2, 2)`, no old `prfl_evol_reps` key, and matching inner `(121, 8)` matrices, and visually checked `prfl_modl_evol_[CFNM_5.316_r52].png`.
- Request: Add PCA analysis for modulation profile evolution using samples over `(IB, t_hold, rep)` with inner `(istp, wavenum)` profiles, restrict wavenum to 0.06-0.6, compute 12 modes, calculate per-mode/per-IB spectra, and add profile-PCA visualizations with IB as an extra parameter while keeping density PCA visuals unchanged apart from folder naming.
  Brief: Added global `modes_pca_prfl_modl` fitting in `script/anlz_excitation_corr.jl` from `prfl_evol` samples shaped `(IB, t_hold, rep)` with profile features `(istp, y_modl_pca)`, using 19 selected wavenum points from 0.0848 to 0.5939 and default `n_pca_modes_prfl_modl=12`. Density PCA spectra now use `selector_t_pca_dens`, profile PCA spectra use `selector_t_pca_modl` and explicit `freq_query_pca_modl`, and `pca_spectra_prfl_modl` is saved as `(mode, IB)` records from `calc_spct_rep_evol`. Added load bindings for the new cache fields. Refactored `src/vispca.jl` with flexible 2D-density and 1D-profile mode drawing helpers plus `set_panel_pca_duet_params!`/`plot_mode_evol_spct_duet_params!`, where per-IB evolution/spectrum rows sit below the shared mode axes. Profile modes render as single-row pos/neg heatmaps, IB row labels are vertical, and fixed tick-label spacing keeps output sizes consistent. Updated correlation visualization so density modes save under `PCA density/<tag_IB>` and profile modes save under `PCA prfl modl/<tag>`. Verified parsing and `git diff --check`, ran `script/anlz_excitation_rerun.jl` with its configured `[06.20].85.Cache.LongTime` input and `[06.20].88.[←85].Dev.PrflModlPCA` output, confirmed profile mode count/weight/spectra sizes `(12)`, `(12, 98, 3)`, and `(12, 12)`, counted 12 generated profile PCA PNGs and 12 density PCA folders, and visually checked one updated profile PCA params figure plus one density PCA figure.
- Request: Statistically check the rerun PCA configuration without running, mirror new rerun configurables back into the fresh runner, and document the rerun-first configuration workflow as a future-agent rule.
  Brief: Added `freq_query_pca_modl = 1:1:100`, `selector_t_pca_dens`, and `selector_t_pca_modl` to `script/anlz_excitation_runner.jl` to match the rerun workflow; kept the same `freq_query_pca_modl` binding in `script/anlz_excitation_rerun.jl`. Removed fallback behavior for `freq_query_pca_modl` in `script/anlz_excitation_corr.jl` and `script/load_excitation_corr.jl` so missing config or metadata fails loudly. Updated `AGENTS.md` with the rule: late-stage rerun configurables may be added to rerun first for iteration, then mirrored back to the runner once stable. Verified by text search and `git diff --check`; no Julia execution was run for this check.
- Request: In multi-parameter PCA mode figures, reduce repeated row clutter by showing x-axis labels only on the final evolution/spectrum row while keeping the rerun script parameters unchanged, then rerun for verification.
  Brief: Added a `show_xlabels` keyword to `plot_pca_evol_spct!` in `src/vispca.jl` and made `plot_mode_evol_spct_duet_params!` pass it only for the last parameter row, hiding intermediate x-axis labels and tick labels. Confirmed there are no per-row axis titles in the params plot beyond the single top figure title. Ran `script/anlz_excitation_rerun.jl` with the user's current `[06.20].89.[←85].Dev.PrflModlPCA` settings, regenerated 12 profile PCA PNGs, and visually checked `CFNM_prfl_modl_1.png`.
- Request: Use newly added sidepeak fidelity series in excitation trend analysis, rename trend internals from `evo` to `evol` and spectrum outputs from `freq`/`ft` to `spct` while keeping `freq_query`, and add variant-separated repeat-overlay trend figures with fidelity-scaled scatter alpha.
  Brief: Updated `src/corr.jl` so `anlz_trend_from_extr` uses `evol_*` locals, stores `evol-all-fit-sp-fidl` and `evol-all-moment-sp-fidl`, writes spectrum outputs as `spct-sel-*`, and weights fit/moment width and wavenum spectra by the matching fidelity vectors. Updated `src/viscorr.jl` and runner trend specs to use `evol_spct`, `spct-*` axes/results, and `width_spct`, with compatibility aliases for old cached `evol_freq`/`width_freq` metadata. Added `set_axis_trend_variant_IB_istp!` and `plot_trend_variant_overlay!` for one-variant-per-file plots with IB rows, istp evol/spct column pairs, PCA-style repeat hues, selected-range shading, and fidelity-driven point alpha. Refined the overlay alpha rule so fit sidepeak properties use fit fidelity, moment uses fidelity only for width/wavenum, envelope size/center ignores fidelity, and evolution ticks are `10:10:100` plus `120:20:220`. Included `script/anlz_excitation_vslz_corr.jl` in rerun, added overlay figure saving in `script/anlz_excitation_vslz_corr.jl`, ran `script/anlz_excitation_rerun.jl` with the current `[06.20].90.[←85].SidePeakFidl` settings, generated 13 `trend_overlay_*.png` files, visually checked `trend_overlay_width_fit_[CFNM].png`, `trend_overlay_height_fit_[CFNM].png`, and `trend_overlay_nvlp-size_fit_size_radial_[CFNM].png`, and verified `git diff --check` passes with only line-ending warnings.
- Request: Fit selected trend evolution properties after `trend_sidepeak_nvlp`, using one oscillation-decay fit per `(IB, istp, property variant)` across all repeats with fidelity weights, cache the fit parameters and relative residue, and draw the fits in the variant overlay figures.
  Brief: Added `fit_evol_oscillation_decay_model` in `src/fitmodels.jl` and weighted fitting helpers in `src/corr.jl` for `A*exp(t/λ)*cos(2π*ν*t/1000 + φ) + C`, with default `ν`, `φ`, and `λ` hints plus per-property user hints from `fit_evol.kwargs`. `script/load_excitation_extr.jl` now preserves rerun-defined `trend_property_specs`, `script/anlz_excitation_corr.jl` builds and saves `fit_evol_properties` as an `(IB, istp)` array of variant-keyed dictionaries, and `script/load_excitation_corr.jl` loads the new cache field when present. Mirrored the current fit specs into `script/anlz_excitation_runner.jl` and fixed the rerun fit spec commas. Updated `src/viscorr.jl`/`script/anlz_excitation_vslz_corr.jl` so overlay evol panels draw black fit curves over the selected time span, spectrum panels mark fitted `ν`, and evol labels report `ν`, `λ`, and `rel_rss`. Ran `script/anlz_excitation_rerun.jl` with `[06.20].91.[←85].SidePeakFit`, confirmed `fit_evol_properties` is saved with shape `(12, 2)` and expected variant keys, regenerated 13 overlay PNGs, visually checked `trend_overlay_width_fit_[CFNM].png`, and verified `git diff --check` passes with only line-ending warnings.
- Request: Add a simulation-adapted excitation runner that loads `.mat` simulation data from `01.[2026.06.01]`, converts time indices and anisotropic grid units into `t_hold`/`px_in_um`, keeps the shared excitation extraction/correlation/visualization scripts with only small adapter changes, skips corner subtraction, uses fixed ROI/core selections, and writes to `Anlz.09.Simu-01.[2025.06.22].[30-100ms].Dev`.
  Brief: Added `script/anlz_excitation_simu_runner.jl` with singleton `IB=[97]` and `rep=1`, selected odd-indexed times in 30-100 ms, fixed crop center `(128, 128)`, `smwh_roi=(127, 127)`, `smwh_core=(127, 76)`, and simulation calibration `unit_t=0.1798`, `unit_in_um=0.2613`. Added simulation `.mat` adapters in `src/loadfmt.jl` to read `nxy_t=*.mat` plus `XY.mat`, sort by time index, compute `px_in_um=(0.0612421875, 0.204140625)`, and format data as `(IB, rep, t_hold, istp)` image-element arrays. Updated `script/anlz_excitation_extr.jl` to accept a runner-provided formatter and optional fixed core center, and updated `src/vispca.jl` so PCA density plots accept anisotropic tuple pixel scales. Tested from the new runner through extraction and correlation cache creation; the initial visualization failed only at PCA plotting due to the tuple-scale annotation, then a cache-based rerun loaded the saved extraction/correlation caches and completed correlation figures plus the full solo table. Output folder contains `SIMU-NTRC_essn_extr.jld2`, `SIMU-NTRC_corr.jld2`, spectrum/trend/PCA figures, and `solo_table_[SIMU-NTRC_97.000_r01].pdf`. `git diff --check` passes with only line-ending warnings.
- Request: Make `draw_solo_modl!` use explicit `dens_max=64.0` and `peak_height_max=3.0` for simulation visualization, while experiment runner and rerun paths pass empty keyword tuples to keep default values.
  Brief: Added runner-level `draw_solo_modl_kwargs`: simulation sets `(; dens_max=64.0, peak_height_max=3.0)`, experiment runner and rerun set `NamedTuple()`. Updated `script/anlz_excitation_vslz_extr.jl` to splat those kwargs into both per-shot and stacked `draw_solo_modl!` calls, with a local empty default for manual includes. Verified by `rg`; no runtime run per user request. `git diff --check` passes with only line-ending warnings.
- Request: Diagnose why simulation property trend graph y-limits for height and weight do not follow the `trend_property_specs` settings in `script/anlz_excitation_simu_runner.jl`.
  Brief: Checked the simulation and experiment runner specs plus the trend plotting code. The simulation `ylim` fields are correctly named and shaped for the current spec-driven path. The current visualization script routes through `plot_trend_all!` and `plot_trend_property!`, which applies `spec.ylim` to evolution axes. The stale/manual plotting helper `plot_trends_sidepeak!` still hard-codes weight/height y-limits, and cache reload via `load_excitation_corr.jl` replaces `trend_property_specs` from saved metadata, so rerunning visualization from an older correlation cache can ignore newly edited runner limits.
- Request: Adapt the simulation excitation runner/loading path from the single-folder `01.[2026.06.01]` dataset to the new `03.[2026.06.10]` dataset, where each `a_22` value lives in an `as22=<value>` subfolder and file names include an ignorable `as22=...` suffix after the time index.
  Brief: Updated `src/loadfmt.jl` so simulation data files match `nxy_t=<time>*.mat`, simulation datasets may be loaded either from the old flat folder or from value folders matching `as22=<value>`, and every configured `IB`/`a22` value maps to the corresponding folder while validating identical time ids across folders. The simulation runner now points at `03.[2026.06.10]`, defines `a22 = [80, 85, 90, 95, 100, 105]` as the first `IB`-compatible axis with `runids` bound to it, and computes density PCA modes from the actual per-IB sample count `rep * t_hold`. Adjusted `gen_run_tag` to prefer scalar `runid` over a retained `runids` vector so per-axis tags become `SIMU-NTRC_<a22>_rNN`. Verified the loader on real `03` data for `a22=[80,85]`, selected first three time points, and confirmed dims `(2, 1, 3, 2)`, ids `[6, 12, 18]`, image `(153, 255)`, and anisotropic `px_in_um=(0.0612421875, 0.204140625)`. A full six-folder run timed out before cache save, so a temporary smoke run with `a22=[80,85]` and `30 <= t <= 40` was used; it completed extraction, correlation, spectra/trends, PCA, overlays, and solo tables in `Anlz.14.Simu-03.[2025.06.22].Smoke`. Restored the runner to the full six-value configuration afterward. `git diff --check` passes with only line-ending warnings.
- Request: Create CFNM and NTRC comparison PDFs from matching profile-modulation SVGs in output folders 95-98, with rows grouped by IB/run ID and source order 96 sansMask, 95 avecMask, 98 sansMask, 97 avecMask; remove comparison-added text and write outputs into the `.98` folder.
  Brief: Added standalone `script/make_excitation_prfl_comparison.mjs` using the IB/run mappings from `script/anlz_excitation_runner.jl`, validating every source SVG and printing vector-preserving HTML/SVG layouts to PDF through local headless Microsoft Edge. Regenerated `comparison.[CFNM].pdf` and `comparison.[NTRC].pdf` in `[06.21].98.FullTime.PrflModlEvol.Tailess.sansMask` as tightly packed SVG-only stacks with no added row/source labels. Verified script syntax, output file timestamps/sizes, and `git diff --check`.
